# LANG-C08C — Native B1/C06 Evidence Re-embodiment

Status: `C08C_NATIVE_B1_C06_RELATION_EVIDENCE_OWNER_EARNED_LIVE_RUNTIME`

## Purpose
C08B proved that the original C01/C06 language lineage was grounded in the research harness but did not persist enough organism-owned raw/action ancestry for native C08 currentness. C08C reconnects that lineage to generic developmental evidence owners already earned later in Microseed.

## Hard controls
The harness may define the body/world and trigger an exogenous event. It may not supply:
- referent groups/classes;
- action-response rows;
- relation order;
- relation digest;
- aligned/reversed labels;
- coordinate meaning or arithmetic;
- predicate/subject/object roles;
- token meaning;
- a relation or token action contract.

## Native mechanism
1. `derive_current_owned_opaque_probe_prefix` reconstructs exact raw observations, action handles, execution ids and evidence ids from current native history.
2. `derive_operational_referent_signatures_from_raw_trace` derives channel groups and opaque action-response signatures internally.
3. `record_current_owned_affordance_effect_profiles` persists both the B1 referent witnesses and directed local raw transition edges for one exclusive opaque action per referent.
4. The external world changes opaque control state without a Microseed action. Two native raw receipts are recorded on either side.
5. `derive_current_owned_passive_raw_transition` accepts the pair only when the durable event order contains no intervening `BOUNDED_ACTION_EXECUTED`.
6. `derive_and_record_current_owned_affordance_relative_directional_relation` compares opaque endpoint equality only. One referent must exactly reverse a known local edge; one must exactly reproduce a known local edge.
7. Relation content identity is the stable digest of relation form + ordered grounded referent signatures. Exact evidence refs remain in the witness ancestry but do not make equivalent fresh evidence a different relation.
8. C08A currentness accepts this native relation witness by evidence id and extracts the relation digest internally.

## Results
- native relation/currentness live run: PASS;
- empirical relation reversal: distinct relation digest + `DRIFT_WITNESS`;
- channel permutation: exact same ordered signatures and relation digest;
- full sensor-sign inversion: exact same ordered signatures and relation digest;
- focused C08C + adjacent owned-currentness: 6/6 PASS;
- MS2046 + C01-C08C: 18/18 PASS;
- native currentness/restart regression: 15/15 PASS;
- competence-loss mutant surface: 5/5 killed, including suppression of C08C native relation derivation.

## Authority ceiling
No semantic predicate, object identity, token meaning, language faculty, generic capability/faculty, selection authority, execution authority or canon promotion is earned. The low-level body action/observation contracts are supplied body interfaces, not new relation faculties.

## Remaining seam
Executable body handlers/contracts are intentionally not reauthorized by restart. Therefore C08C does not claim post-restart native relation re-derivation. That is the next explicit frontier, followed by C07 token-binding re-embodiment on the native relation witness.

## Laws
`HARNESS_GROUNDED_RELATION != ORGANISM_OWNED_RELATION_ANCESTRY` remains the C08B scar.

C08C adds the bounded engineering/science distinction:
`RELATION_CONTENT_DIGEST != EVIDENCE_INSTANCE_DIGEST`.

And the positive-path pressure remains:
`CORRECT_ABSTENTION != DEMONSTRATED_COMPETENCE`.

# LANG-C08E — Native C07 Token-Binding Re-embodiment

Status: `C08E_NATIVE_C07_TOKEN_BINDING_REEMBODIED_ON_POSTRESTART_NATIVE_RELATION`

C08E repairs C07 lineage rather than adding a new organism faculty. Opaque external token bytes are content-bound in Microseed evidence. Native C08D relation witnesses are content-bound/current-runtime evidence. The relation witness is immediately followed by the token observation in `EvidenceLedger.list()` append order; a derived pair witness persists exact relation/token evidence refs. Training and independent holdout pair-evidence sets must agree on an exact two-token/two-relation bijection. The resulting bindings are embodied through the already-existing generic `OpaqueEvidenceAssociationRegistry`.

After restart the association records replay as `REVALIDATION_REQUIRED`. Durable binding/token bytes are insufficient: an old relation witness is rejected by C08D current-runtime rules. Explicit body reattachment plus fresh post-BOOT organism-owned experience re-derives the native relation, after which the correct opaque token resolves and revalidates. Wrong relation and unseen token fail closed. Holdout convention reversal and ambiguity fail closed. Token-surface permutation and sensor representation transformations preserve the empirical association structure.

No `microseed/` delta is required. No token action, relation action, predicate registry, meaning registry, semantic roles, language module, or generic capability/faculty is added. External train/holdout partition remains qualification assistance and is not claimed endogenous.

Important integrity ceiling: `EvidenceLedger.list()` is an operational append-order surface, but the current evidence ledger is not predecessor-chained. Therefore `LEDGER_ORDERED_PAIRING != TAMPER_EVIDENT_SEQUENCE_PROOF`. C08E earns the bounded runtime/evidence result under the current ledger, not cryptographic sequence integrity.

Verification: C08E+C08D+C07 8/8 PASS; MS2046+C01-C08E 23/23 PASS; native currentness/restart 15/15 PASS; positive-path loss mutants 7/7 killed.

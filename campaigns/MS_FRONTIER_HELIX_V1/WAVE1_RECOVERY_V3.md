# Wave-1 Recovery Runtime V3

V3 resumes from the reconciled V2 branch heads and exact admitted-pass bundles. Existing `RESEARCH_STOP.json`, `ARM_CLOSEOUT.json`, HELIX and pass files are immutable historical evidence. V3 writes append-only `*_V3.json` state/stop/session receipts instead of overwriting them.

Initial successor questions are explicit external research-control reframes, not scientific evidence. They are designed to break local-model saturation by asking narrower differential discriminators. Subsequent questions still must be earned by admitted passes under Semantic HELIX.

PDVER remains exactly `PROBE -> DERIVE -> VERIFY -> EMBODY -> RECURSE`; OARR, LOOP+, HSP, Reservoir and CSC remain active. Production mutation and promotion authority remain NONE.

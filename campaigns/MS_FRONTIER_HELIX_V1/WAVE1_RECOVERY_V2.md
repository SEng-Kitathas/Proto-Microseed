# Wave-1 Recovery Runtime V2

Purpose: resume sealed exploratory arm histories without replaying admitted passes and without weakening R3.1 gates.

Changes:
- authoritative `question` is control-plane input; model echo mismatch is preserved in raw output but cannot invalidate science by itself;
- every raw model response is retained; malformed JSON is retried, and a narrowly-scoped syntax-only invalid-backslash repair may be used with both raw and repaired text preserved;
- repeated/self-loop successor generation is not a Python failure and not a scientific pass: it becomes `QUESTION_SATURATED__REQUIRES_EXTERNAL_REFRAME`;
- terminal pass language requires CSC PASS + terminal_supported before arm closure; CSC REVIEW triggers a separately earned successor reframe or explicit research stop;
- exact existing pass chains remain immutable and resume from the last valid NEXT;
- production mutation remains forbidden and promotion authority remains NONE.

`STRICT_GATE != BRITTLE_TRANSPORT`. The V2 repair changes research-instrument handling, not Microseed scientific law.
- V2 artifacts use distinct filenames so sealed V1 rejected attempts are never overwritten.
- successor reframing retries the same pending scientific pass number; only an admitted pass increments Pxx.

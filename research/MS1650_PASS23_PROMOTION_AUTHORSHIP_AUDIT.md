# MS1650 / Pass 23 — Promotion + authorship audit

The surviving implementation remains native to the existing Microseed ownership pattern: capability contracts carry immutable content identity; the entity facade wires an optional historical-admission basis into the already-existing assured outcome ingress; action learning carries generic evidence-premise epoch/signature ancestry through candidate, holdout qualification, relation, and currentness.

No manager, registry, scheduler, sensor-specific state machine, truth authority, or execution authority was added.

## Promotion verdict

**Research lineage: SURVIVES. Main-Dev: DEFER.**

The mechanism now separates:

- live observation access;
- historical evidence-admission validity;
- prospective mapping-content change;
- retrospective admission falsification;
- restart identity aliasing;
- applicability of an old historical basis to a new acquisition.

But the historical-admission basis is still externally qualified. A basis may be current, correctly content-bound, and applicable yet physically wrong/incomplete. Capability content signatures deliberately exclude runtime handler implementation, so implementation/world correctness remains a separate currentness/grounding responsibility.

Therefore:

`CONTENT_BOUND_APPLICABILITY != PHYSICAL_CORRECTNESS != EXHAUSTIVE_COMPLETENESS`

The correct move is to preserve the mechanism as research quarry, not promote it as solved epistemology.

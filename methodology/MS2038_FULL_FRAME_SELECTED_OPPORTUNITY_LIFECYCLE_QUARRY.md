# MS2038 — FULL-FRAME SELECTED OPPORTUNITY LIFECYCLE QUARRY

## Goal
Determine whether a current strict full-frame Pareto winner can traverse the already-earned endogenous UNKNOWN -> durable ACTION_LIMITED deficit -> ordinary epistemic intent lifecycle without adding a scheduler, opportunity registry, or execution authority.

## Research-only adapter
1. Re-derive the runtime current full-frame cross-deficit selection surface.
2. Require `CURRENT_STRICT_FULL_FRAME_CROSS_DEFICIT_SELECTION` with authority `STRICT_FULL_FRAME_PARETO_REGULATORY_DOMINANCE_ONLY`.
3. Recover the exact selected current owned referent opportunity from the runtime-owned opportunity set.
4. Materialize one endogenous `UNKNOWN_INCOMPLETE` evidence record bound to:
   - selected ephemeral deficit/trial/binding/probe;
   - current frame digest;
   - full-frame selection commitment id/authority;
   - priority/information/local-step commitments;
   - opportunity content signature.
5. Persist the selected deficit with explicit assistance marker `ENDOGENOUS_UNKNOWN_MATERIALIZED_AFTER_STRICT_FULL_FRAME_PARETO_SELECTION`.
6. Delegate nomination to the ordinary current epistemic step nomination owner.

## Hostiles
- full-frame trade-off -> ABSTAIN, zero durable deltas;
- full-frame P2 dominance -> exactly one endogenous UNKNOWN, one durable deficit, one P2 intent, zero executions;
- second identical call -> idempotent ABSTAIN, zero deltas;
- current constitutional value X without observation -> no complete frame, zero durable deltas;
- handler calls remain zero.

## Nonclaims
No execution, effect-time currentness, runtime promotion, scheduler, semantic value hierarchy, or PRELINGUAL_SUBSTRATE_V1 promotion is claimed.


## First run — HARNESS IMPORT FAILURE
The first direct run failed before scientific execution because the scratch adapter imported `action_result_digest` from `microseed.runtime.commitment`; the live symbol is imported/exported through `microseed.runtime.entity` from the action-closure digest owner.

Classification:
`MS2038_HARNESS_IMPORT_BINDING_FAILURE__NO_SCIENTIFIC_VERDICT`.

Per research lineage policy this failed state is preserved before repair.


## Repair 1 — bind live action digest owner
Narrow repair only: import the live `action_result_digest` alias from `microseed.runtime.entity`, matching historical scratch lineage. No lifecycle hypothesis changed.


## Observed result — FULL-FRAME SELECTED LIFECYCLE EARNED
After the published import-binding failure, Repair 1 bound the live action digest owner only. Direct witness PASS.

Observed:
- full-frame trade-off -> ABSTAIN with zero durable deltas;
- full-frame P2 dominance -> one endogenous UNKNOWN_INCOMPLETE, one durable ACTION_LIMITED deficit, one P2 intent, zero executions;
- second identical call -> idempotent `SELECTED_EPISTEMIC_DEFICIT_ALREADY_PERSISTED`, zero deltas;
- current X without observation -> no complete frame, zero durable deltas;
- handler calls zero.

The selected UNKNOWN is content-bound to the full-frame selection commitment id/authority and frame digest, while the durable deficit carries `ENDOGENOUS_UNKNOWN_MATERIALIZED_AFTER_STRICT_FULL_FRAME_PARETO_SELECTION`.

Cleanup-neutral focused MS2036–MS2038 lineage: **10/10 PASS in 53.21s**, stderr empty.

Earned research-only lifecycle:
`STRICT_FULL_FRAME_SELECTED_EPHEMERAL_OPPORTUNITY_CAN_USE_EXISTING_ENDOGENOUS_UNKNOWN_AND_ORDINARY_NOMINATION_LIFECYCLE`.

Execution/effect-time currentness remains unearned.

# MS2037 — PROMOTE FULL-FRAME PARETO READ-ONLY SURFACE

## Goal
Promote the MS2035–MS2036 research composition into existing production owners without introducing a new manager or granting persistence/EFFECT authority.

## Owner placement
- `microseed/development/value.py`: complete current constitutional value-frame derivation from `ValueVariableRegistry`.
- `microseed/development/epistemic_priority.py`: frame-bound branch-conditioned cross-value consequence vector and strict full-frame Pareto commitment.
- `microseed/runtime/entity.py`: current wiring only — current owned referent opportunities + current singleton action/value effect witnesses + read-only surface.

## Required invariants
1. Runtime caller supplies no value-coordinate subset.
2. Every current constitutional value requires a current observation before the frame is complete.
3. Every branch-selected downstream action requires a current singleton effect witness on every frame coordinate.
4. Value/capability epoch drift fails closed.
5. Vector rows bind exact frame digest and exact frame descriptors.
6. Strict Pareto winner only when one opportunity is no worse on every current frame coordinate and strictly better on at least one.
7. Trade-offs/ties/incomplete vectors -> UNKNOWN/no selection.
8. No persistence, nomination, handler invocation, execution, truth, semantic-goal, or semantic-value-priority authority.

## Hostiles
- P2/P4 full-frame trade-off -> no selection.
- full-frame P2 dominance -> one read-only P2 selection.
- current value added without observation -> no frame/no selection.
- same-epoch value observation drift between vector derivation and comparison -> stale/no selection.
- missing action/value effect -> no selection.
- runtime public surface has no caller value-id argument.
- historical same-value surfaces remain unchanged.

## Nonclaims
MS2037 does not authorize durable selected UNKNOWN materialization, nomination, execution, or effect-time reauthorization. Those require later campaigns.


## Observed result — PROMOTED READ-ONLY SURFACE GREEN
Production owners now contain the minimal read-only composition:
- `development/value.py` owns complete current constitutional value-frame derivation;
- `development/epistemic_priority.py` owns frame-bound consequence vectors + strict full-frame Pareto commitment;
- `runtime/entity.py` owns current wiring over owned referent opportunities + current singleton action/value effects.

Focused cleanup-neutral lineage: **16/16 PASS in 54.75s**, stderr empty.

Observed:
- full-frame trade-off -> no selection;
- full-frame P2 dominance -> `STRICT_FULL_FRAME_PARETO_REGULATORY_DOMINANCE_ONLY`;
- current unobserved X -> no complete frame/no selection;
- same-epoch observation drift changes production frame digest;
- public runtime surface has no caller value-subset parameter;
- historical same-value selection surface remains green;
- no handler calls, persistence, nomination, or execution.

MS2037 earns a production **read-only** selection surface only. Durable lifecycle/effect-time authority remains unearned.

# MS1962–MS1963 — Referent Continuity: Organism-Lineage Non-Substitution and Overlap Bridge

Date: 2026-08-29 ET
Status: research/reality boundary results; no new Microseed-core mechanism beyond MS1958–1961 signature owner
Parent: MS1958–1961 `c92d2eae21823ff695457d0ca7205c5db431f5aa`

## MS1962 — organism continuity is not referent continuity
Scratch:
`scratch/ms1962_organism_continuity_not_referent_identity.py`

Job `job-3a012e461682` PASS.

The same organism biography was extended from one proto-referent session to a later alias-swapped session.

Existing `DevelopmentalBiography.relation(...)` returned:
`DESCENDANT_CONTINUATION`.

Existing `DevelopmentalContinuityWitness` returned:
- `BRANCH_RELATIVE_DESCENDANT_CONTINUATION`;
- lineage authority `OPERATIONAL_DEVELOPMENTAL_LINEAGE_ONLY`;
- numerical identity authority NONE;
- selfhood NOT_QUALIFIED.

Yet the exact same local proto-referent signatures aligned to opposite evaluator-only external latent sources across the joint sensor+actuator alias swap.

Earned:
`ORGANISM_DEVELOPMENTAL_CONTINUITY_DOES_NOT_BREAK_EXTERNAL_REFERENT_ALIAS_SYMMETRY`.

Required breaker:
`REFERENT_SPECIFIC_CAUSAL_CONTINUITY_OR_ASYMMETRIC_OVERLAP_EVIDENCE`.

This prevents a category error:
`ORGANISM_LINEAGE != EXTERNAL_REFERENT_LINEAGE`.

## MS1963 — referent-specific overlap evidence
Reality world:
`research/substrate_shadow/referent_handoff_world_server.py`.

The world maintains the same two external latent sources while moving through three observation phases:
- OLD sensor layout;
- OVERLAP exposing old+new layouts simultaneously;
- NEW sensor layout.

No semantic object labels are supplied to the referent mechanism.

Scratch:
`scratch/ms1963_referent_specific_overlap_continuity.py`

Job `job-772bec4a7c1c` PASS.

Results:
- OLD recovered two boundary/affordance groups;
- OVERLAP recovered two groups, each containing channels from both the old and new representations;
- NEW recovered the two groups under the new channel layout;
- the same affordance-relative signatures existed in all three phases;
- evaluator-only scoring showed each signature tracked the same latent causal source old -> overlap -> new.

Example bridge:
- signature `314f...` old `(0,1)` -> overlap `(0,1,5,7)` -> new `(1,3)`;
- signature `7497...` old `(2,3)` -> overlap `(2,3,4,6)` -> new `(0,2)`.

Earned:
`REFERENT_SPECIFIC_OVERLAP_EVIDENCE_CAN_BRIDGE_OPERATIONAL_PROTO_REFERENT_REPRESENTATIONS_ACROSS_SENSOR_HANDOFF`.

Authority ceiling:
- continuity authority `OPERATIONAL_REFERENT_CONTINUITY_ONLY`;
- numerical identity authority NONE;
- semantic reference authority NONE.

Preserve:
`OVERLAP_BOUND_OPERATIONAL_CONTINUITY != NUMERICAL_OBJECT_IDENTITY`.

## Language-gate consequence
Microseed now has evidence for three distinct levels that must not be collapsed:
1. within-session partition by boundary coherence;
2. cross-session re-association by stable affordance-relative content;
3. explicit representation continuity when old/new sensor views overlap over the same causal responses.

None establishes metaphysical/numerical object identity or linguistic reference. But they materially narrow the prelingual grounding problem by showing that useful referential continuity can be operational and evidence-bound rather than label-supplied.

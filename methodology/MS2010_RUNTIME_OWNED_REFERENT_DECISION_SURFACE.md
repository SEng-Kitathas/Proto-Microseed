# MS2010 — RUNTIME-OWNED REFERENT DECISION SURFACE

## Purpose
Embody the MS2005/MS2007/MS2009 composition inside the existing `Microseed` owner as read-only runtime derivations so later execution can rederive its premises without caller-supplied trace or alternatives.

## Added runtime projections
- `derive_current_owned_opaque_probe_prefix`: current raw receipts + action/outcome predecessor chain -> bounded chronological raw/action prefix.
- bounded current operational signature-class scan and class-set bucket reconstruction.
- `derive_current_partial_operational_referent_ambiguity`: owned prefix + current qualified routing buckets -> live survivors + opaque informative-probe surface.
- `derive_current_owned_referent_decision_surface`: exact current referent deficit + projection/hypothesis ancestry -> current relation sets, exact unique probe relation ancestry and current discriminator signature.

These persist no new registry state and grant no authority. They are read-only projections over existing evidence/currentness owners.

## Hostiles
A real ordinary P0→P1 prefix is executed and observed inside the MS2008 regulatory fixture. The runtime must recover the prefix, derive both live buckets and unique P2, then reconstruct the exact current decision surface. A deficit carrying a forged/stale discriminator digest must be rejected as content drift.

## Boundary
MS2010 does not execute P2. It only makes the current premises runtime-owned and freshly reconstructible.

# Substrate Hardening V1 — H Grounded-Binding Empirical Currentness Gate

Status: VERIFIED GATE BLOCKER / CSC BLOCK FOR CO-REFERENCE ADVANCE / NO CANONICAL PRODUCTION DELTA

## Hostile
A grounded SIG-X binding is earned against referent P. The external convention then reverses to referent Q without changing the signal capability or coordination descriptors. Fresh grounded train+holdout evidence independently earns Q.

The old research binding helper still reports `CURRENT_OPERATIONAL_TOKEN_REFERENT_BINDING_CANDIDATE` because it checks structural descriptors only. Mixed old-train/new-holdout evidence correctly returns `DEFER_UNKNOWN / HOLDOUT_REFERENT_BINDING_DISAGREES`.

Combined frontier/currentness guard: 11/11 PASS.

## Law
`STRUCTURAL_TOKEN_BINDING_CURRENTNESS != EMPIRICAL_GROUNDED_REFERENCE_CURRENTNESS`.

## Disposition
Do **not** advance H into durable co-reference yet. First earn a post-binding empirical currentness story for grounded token→referent bindings, preferably by composing existing grounded-use/provenance/currentness owners. No language/reference manager is authorized.

## CSC adjudication
CSC returned BLOCK with finding_supported=true, scope_correct=true and co_reference_should_remain_blocked=true. Its `production_defect=true` field is rejected by R1 as a category error: `derive_binding_candidate` / `binding_status` are research/scratch candidate machinery and are not canonical production language owners.

Language/truth/semantic-reference/execution authority remain NONE.

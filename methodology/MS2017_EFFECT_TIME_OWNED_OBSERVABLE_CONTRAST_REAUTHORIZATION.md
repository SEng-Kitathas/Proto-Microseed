# MS2017 — EFFECT-TIME OWNED OBSERVABLE CONTRAST REAUTHORIZATION

## Question
Does the MS2016 owned observable-partition information commitment survive the ordinary effect-time reauthorization boundary without trusting caller-supplied decision context?

## Required positives
- nominate P2 in the same-control-state hostile using owned raw contrast information;
- pass a forged caller context at execution that collapses the alternatives;
- ordinary `execute_bounded_action` must ignore that caller context for the owned-referent lineage, re-derive the current referent surface and observable contrast internally, reproduce the current epistemic commitment, and execute P2 exactly once.

## Required negatives
- after nomination, add a duplicate current raw receipt so the owned prefix is no longer exact;
- effect-time reauthorization must stop before the P2 handler with no execution;
- caller-supplied context may not rescue the stale/ambiguous owned evidence.

## Authority
No new executor, no cached information authority, no truth/identity/model-switch authority. `AVAILABLE/NOMINATED != CURRENT_AT_EFFECT != EXECUTED` remains explicit.

# MS2023 — STRICT SAME-VALUE CROSS-DEFICIT SELECTION COMMITMENT

## Goal
Pressure the MS2022 quarry as an explicit bounded relational commitment before promoting anything into the runtime.

## Law
Cross-deficit selection may become YES only when freshly re-derived current opportunities:
- are each individually decision-bearing, observable-information-bearing, feasible and step-licensed;
- share the exact same regulatory `(value_id, value_epoch)` and current value observation;
- yield current one-step rehearsal consequences;
- have exactly one opportunity with strictly lower worst residual pressure than every other live opportunity.

Tie, cross-value mismatch, missing current consequence surface, or changed current value that erases strict dominance => UNKNOWN / no selection.

## Authority
The commitment may grant only `STRICT_SAME_VALUE_REGULATORY_DOMINANCE_ONLY` selection authority. It grants no execution, truth, semantic-goal, cross-value-ranking, scheduler, or weighted-utility authority.

## Hostiles
- symmetric MS2021 P2/P4 remains UNKNOWN;
- asymmetric MS2022 same-value fixture selects P2;
- after changing the current V observation into the viable interval, re-derived comparison must no longer select P2;
- no handler calls occur.

## First execution result — HARNESS FAILURE, preserved
The first direct run did not reach the scientific discriminator. `derive_selection_commitment()` attempted to read `op["step_commitment"]`, but the live MS2021 opportunity object exposes the already-licensed per-opportunity commitment under `op["commitment"]`; `step_commitment` exists only in the later serialized summary surface. The run raised `KeyError: 'step_commitment'` before a cross-deficit commitment was derived. A subsequent Windows `TemporaryDirectory` SQLite cleanup lock was secondary.

Classification: `CAMPAIGN_HARNESS_SHAPE_MISMATCH__NO_SCIENTIFIC_VERDICT`. This state is intentionally committed before repair so the research lineage remains unbroken.

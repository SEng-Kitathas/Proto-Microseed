# Microseed Main-Dev Operating Profile v2.0

Additive profile through **MS1302**. It inherits v1.9 and preserves the full HSP → Loop+/Research → OARR → PDVER → Research+Embodiment → Semantic Helix → Attention Reservoir → Main-Dev stack.

## Earned MS1278–1302 refinement

Predictive drift may now be narrowed by comparing one stale robust projection against one **externally qualified** alternative constructor on a bounded supplied comparison set. A positive result establishes only `ALTERNATIVE_STRUCTURE_SUPPORTED`: a different opaque predictive structure fits materially better within supplied bounds. It does **not** identify semantic drift cause, noise, nuisance, context, or regime identity. Failure to find a distinct alternative remains `UNKNOWN_CAUSE`; it is never converted into “therefore noise.”

A **named** stale historical projection may also be tested for recurrence using supplied operational ordering, fixed windows, accuracy floor, action-only baseline lift, and consecutive-success count. `RECURRENCE_EVIDENCE` means that historical opaque predictive law is again operationally useful within bounds. It does not mean the same regime or context has returned. The witness has no reactivation authority.

Projection currentness may return only after **fresh external recurrence requalification**. Reactivation creates a new projection epoch, preserves prior stale history, and does not reactivate old contrast bindings. Frame currentness remains mandatory; temporal projections additionally require current EpisodeSchema ancestry. Restart preserves the operational witness/currentness record without identity gain.

## Hard ceilings

- `NO_ALTERNATIVE_STRUCTURE_WITHIN_BOUNDS != NOISE_IDENTIFIED`.
- `ALTERNATIVE_STRUCTURE_SUPPORTED != DRIFT_CAUSE_IDENTIFIED`.
- `RECURRENT_PREDICTIVE_LAW != RECURRING_REGIME_IDENTITY`.
- `RECURRENCE_EVIDENCE != REACTIVATION_AUTHORITY`.
- External qualification/requalification remains mandatory.
- No global historical-regime search or automatic model switcher.
- No semantic noise model, context ontology, regime identity, drift-cause classifier, self-qualification, language, or numerical selfhood.
- Passive evidence may remain insufficient for cause identification; UNKNOWN is required.

## Current hard stop

- Research terminal: **MS1302**
- Integration terminal: **MS1302**
- **MS1303 NOT STARTED**
- Selected frontier: `ATTN-MS1302-DISCRIMINATING-INTERVENTION-FOR-DRIFT-CAUSE-IDENTIFIABILITY__PRELINGUAL-CHANGE-CAUSE-PROBING`

Next pressure: determine whether bounded discriminating interventions can separate alternative explanations for predictive drift without importing semantic cause labels or a general active-learning planner.

# MS2015 — OBSERVABLE PARTITION OWNER QUARRY

## Question
Does the substantive blocker require a new representation owner, or can the existing `EpistemicContrastRow` lawfully represent a raw-observation discriminator even when the probe has identical control-state consequences across live alternatives?

## Hostile
Keep the current referent ambiguity decision-bearing: bucket A prefers downstream action A and bucket B prefers B. Keep the owned raw response model such that P2 is uniquely informative. Force the qualified P2 action-outcome relation to predict the same opaque control state `s0` under both alternatives.

Required observations:
- current decision-bearing commitment remains YES;
- generic state-trace program information remains NO (`PROGRAM_CANNOT_CHANGE_OBSERVABLE_EVIDENCE`);
- an `EpistemicContrastRow` built from the owned P2 raw-response partition contains distinct opaque outcome digests for the two candidate buckets;
- its serialized payload contains no state/action/next-state/value transition-model fields;
- `derive_pre_evidence_discriminator_signature` is content-sensitive to those raw-response outcome digests.

## Interpretation
If this passes, `EpistemicContrastRow` is already the correct representation owner. The blocker becomes missing wiring between a current owned action-conditioned contrast and program-information licensing, not a missing sensor model, referent manager, state split, or planner.

## Authority
Research quarry only. No execution, selection, truth, semantic identity/reference, model-switch, or canonical authority.

# MS2029 — PROMOTE SELECTED OPPORTUNITY PERSISTENCE + ORDINARY NOMINATION

## Goal
Promote the repaired MS2024 lifecycle composition into `Microseed` after MS2028 made current opportunity/selection derivation runtime-owned.

## Runtime behavior
`nominate_current_strict_same_value_referent_epistemic_opportunity()`:
1. Re-derives the current owned opportunity set and strict MS2027 selection.
2. Requires one current YES selection and exact selected deficit/probe match.
3. Materializes one deterministic endogenous `UNKNOWN_INCOMPLETE` evidence record content-bound to the selected opportunity, raw provenance, trial/commitments and cross-deficit selection commitment.
4. Persists only that already-derived ACTION_LIMITED deficit using the existing deficit owner.
5. Delegates action-intent nomination to the existing current-surface epistemic nomination owner.

## Laws
- tie/no current selection => no evidence/deficit/intent/execution;
- self-derived UNKNOWN is proposal/unknown evidence, never external support;
- selection authority remains `STRICT_SAME_VALUE_REGULATORY_DOMINANCE_ONLY` and grants no EFFECT;
- no persistent opportunity registry/scheduler is created;
- repeated nomination is idempotent at the deficit boundary and must not duplicate durable state.

## Hostiles
- symmetric P2/P4 tie -> ABSTAIN, zero durable deltas;
- asymmetric strict P2 -> exactly one UNKNOWN, one durable deficit, one P2 intent, zero execution;
- second call -> `SELECTED_EPISTEMIC_DEFICIT_ALREADY_PERSISTED`, zero new deltas;
- value drift before nomination -> no current opportunity and zero durable deltas.

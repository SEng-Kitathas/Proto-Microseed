# MS2008 — REFERENT AMBIGUITY BECOMES DECISION-BEARING

## Question
Can the live partial referent ambiguity earned in MS2007 become a lawful epistemic initiation pressure because the surviving qualified referent buckets imply different current regulatory actions?

## Composition
- MS2007 derives surviving qualified buckets from current partial raw response content.
- Existing qualified projection-conditioned routing maps each surviving bucket to already-qualified action-outcome relations.
- Existing lossless `as_epistemic_alternative_relation()` projects those qualified relations into an ephemeral decision surface.
- Existing value/currentness/feasibility/priority owners decide whether the alternatives would choose different current actions.
- Existing program-information owner asks whether P2 produces different observable traces.

No curiosity scalar, referent planner, scheduler, model manager, or semantic identity is introduced.

## Fixture
Current V pressure is below its viable interval. In referent bucket A, action A repairs pressure and B does not. In referent bucket B, B repairs pressure and A does not. P2 has zero direct regulatory effect but predicts different opaque next states across the two qualified buckets. Current P0/P1 evidence leaves both buckets live.

## Pass
- live survivor set derived, not supplied;
- qualified routing produces two current relation sets;
- decision-bearing priority = YES with first actions {A,B};
- P2 program information = YES;
- one inert `BoundedActionIntent` may be nominated through the existing epistemic lane;
- no handler is invoked.

## Authority
Decision bearing and information do not grant truth, selection, execution, identity or semantic-reference authority.

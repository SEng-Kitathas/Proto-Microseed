# MS1997 — Authenticated Lived History to Endogenous Program Bridge

Date: 2026-08-30 ET
Status: VERIFIED / ready for local seal and research-branch publication
Parent: published MS1996 `c5c84cfbd308df38a94144b0bd4a9eed004d3a81`

## Question
Can the already-earned three-locus endogenous program machinery consume **actual ordinary effect executions and bounded observed outcomes** from one persistent Microseed state, rather than the synthetic action-closure records used in the earlier MS1820/MS1996 research fixtures?

Prewrites:
- `SYNTHETIC_OWNED_RECORD != LIVED_EXECUTION_HISTORY`;
- `AUTHENTICATED_EXECUTION + OBSERVED_OUTCOME != TRUTH_AUTHORITY`;
- `CONTROL_EVIDENCE_SUCCESSOR_ANCESTRY != EPISODE_SEMANTICS`;
- `EQUIPPED_TRAINING_ASSISTANCE != ENDOGENOUS_PROGRAM_SELECTION`;
- `MODE_CONDITIONED_ASSISTANCE != RICH_WORLD_AUTONOMY`.

## Why this campaign comes first
Published MS1996 established caller-free program construction from owned history, but its hostile fixture populated the required action/outcome records directly. Before adding restart, delay, drift, liveness, referent, and resource pressure into one long world, the history-to-program seam must first survive contact with ordinary execution and observation ingress.

If it failed here, adding a large rich-world harness would hide the missing bridge inside complexity.

## Existing mechanism path
No new production mechanism is added.

The path under pressure is:
1. ordinary bounded rehearsal proposal;
2. current regulatory action commitment;
3. ordinary bounded action intent;
4. qualified EFFECT invocation;
5. bounded observation-use basis;
6. ordinary `record_bounded_action_outcome(...)`;
7. action closure updates the current opaque control-state witness to the new outcome evidence ID;
8. the next ordinary action intent inherits that evidence ID as `control_state_evidence_id`;
9. `derive_action_outcome_successor_couplings()` recovers exact immediate-successor ancestry only from that content-bound evidence link;
10. recurrent alternatives + successor couplings form complete three-locus chains;
11. the unchanged MS1996 generator searches the resulting proposal-only alternative surface.

The critical existing mechanism is therefore already present:
`PREVIOUS_OUTCOME_EVIDENCE_ID == NEXT_INTENT_CONTROL_STATE_EVIDENCE_ID`.

No episode manager or synthetic successor list is introduced.

## World
Scratch:
`scratch/ms1997_lived_history_to_endogenous_program.py`

One external opaque world exposes three qualified EFFECT handles:
- `K-17`;
- `M-23`;
- `R-41`.

Two evaluator-only causal modes, P and N, produce the same first two opaque control-state transitions:
- `s0 -> s1`;
- `s1 -> s2`.

The terminal transition differs operationally:
- P: `s2 -> u`;
- N: `s2 -> v`.

The scalar regulatory effects are also opposite:
- P: `+1` each step, starting from `-3`;
- N: `-1` each step, starting from `+3`.

Both trajectories reduce current bounded regulatory pressure toward the same viable interval and finish at observed value `0.0`.

Four lived episodes are executed in one persistent Microseed state:
- P;
- P;
- N;
- N.

Each episode contains three real effect executions and three real bounded outcomes.

Total:
- 12 ordinary executions;
- 12 ordinary outcomes;
- 12 action-outcome experiences.

## Training assistance ceiling
MS1997 deliberately does **not** claim full rich-world autonomy.

The training action nominations use supplied one-step rehearsal rows. All three current opaque action options are present on each nomination and represented current state determines the applicable one-step relation, so the caller does not directly pass the preferred action handle.

However, those supplied rehearsal rows are conditioned on the external world's current causal mode. This is explicit EQUIPPED assistance.

Therefore:
`MODE_CONDITIONED_TRAINING_ASSISTANCE == YES`.

This assistance is not counted as organism-owned history, physical truth, semantic mode identity, or execution authority. The evaluator's P/N causal label is not written as a durable organism evidence field. The organism does observe the actual consequences, as it must.

MS1997 earns only the lived-history bridge. Removing mode-conditioned training assistance remains a required rich-world pressure.

## Result
From the 12 authenticated lived outcomes, unchanged current mechanisms derive:
- alternative hypotheses: 6;
- recurrent successor couplings: 4;
- complete three-locus chains: 2;
- alternative relation sets: 2.

The successor coupling evidence is not supplied by the harness. For every recovered pair, the next execution's intent carries the preceding outcome evidence ID as its exact control-state ancestry.

After returning to the common represented root `s0`, the existing MS1996 generator returns:
- status `REPRESENTED_INFORMATIVE_PROGRAMS_FOUND`;
- generated opaque program `K-17 -> M-23 -> R-41`;
- execution authority NONE;
- truth authority NONE;
- qualification authority NONE;
- closure authority NONE.

No synthetic `BoundedActionIntent`, `ActionExecutionRecord`, or `ActionOutcomeRecord` is inserted by the MS1997 harness.

## Earned
`AUTHENTICATED_LIVED_ACTION_OUTCOME_HISTORY_CAN_DIRECTLY_FORM_THE_EXISTING_THREE_LOCUS_ALTERNATIVE_SURFACE_AND_ENDOGENOUSLY_GENERATE_THE_DISCRIMINATING_OPAQUE_PROGRAM_WITHOUT_SYNTHETIC_ACTION_CLOSURE_RECORDS`.

## Mechanism verdict
**No new core mechanism required.**

The ordinary action/outcome owner already carries the exact successor ancestry needed by the endogenous three-locus program machinery.

This rejects a new history-bridge manager.

## Authority ceiling
- lived observation: observation-only;
- alternative surface: proposal-only ephemeral;
- generated candidate: proposal-only ephemeral;
- execution authority from generation: NONE;
- truth authority: NONE;
- hidden causal-mode semantics: NONE;
- numerical identity: NONE;
- language authority: NONE.

## What MS1997 does not solve
MS1997 is **not** the full rich-world lifetime campaign.

Still open:
1. remove mode-conditioned training assistance;
2. make useful action/program construction depend on organism-owned learned history under one long developmental trajectory;
3. combine delayed settlement;
4. combine noise/drift and relearning;
5. combine endpoint liveness / ambiguous dispatch;
6. combine restart and currentness reauthorization;
7. combine external referent crossing/occlusion/reassociation;
8. combine bounded resource/search pressure;
9. preserve explicit abstention/unknown rather than campaign-side rescue.

The next campaign must enforce:
`CAMPAIGN_ORCHESTRATION != COGNITIVE_SELECTION`.

## Remaining boundary
`REMOVE_MODE_CONDITIONED_TRAINING_ASSISTANCE_AND_COMPOSE_DELAY_DRIFT_LIVENESS_RESTART_REFERENT_AND_RESOURCE_PRESSURE_IN_ONE_SUSTAINED_WORLD`.

## Final verification
- direct MS1997 scratch boundary: PASS;
- focused lived-history / action-outcome / endogenous-program lineage `job-877f1e2b917b`: **16/16 PASS in 8.14s**;
- Microseed self-test: **81/81 PASS**;
- compileall: PASS;
- `git diff --check`: PASS;
- whole cleanup-neutral embodiment suite `job-204b443d5825`: **795/795 PASS in 597.16s**;
- whole-suite stderr: empty;
- scratch/test/methodology hashes were frozen before focused/whole verification and all three matched exactly after the whole-suite pass.

## Seal/publication gate
The pass is eligible to seal. Publication still requires local Git seal, exact research-branch push, and independent remote ref readback matching the seal.

# MS2043 — FOUR-REFERENT PARTIAL-OBSERVABILITY SCALE

## Goal
Pressure the existing operational referent substrate beyond the two-referent MS1995 case without introducing object IDs, trackers, semantic classes, or genealogy.

## World
Four latent evaluator-only sources A/B/C/D, each represented by two visible channels when fully exposed.

Phases:
- PRE: four paired groups;
- CROSS: all four groups interleave and move presentation position;
- OCCLUDE_AC: only B and D remain visible;
- OCCLUDE_BD: only A and C remain visible;
- GAP: no channels;
- POST: all four return under a new permutation and affine appearance transform.

Opaque action schedule includes source-specific effects A/B/C/D plus one global effect. Existing boundary coherence + affordance-relative signatures must recover operational groups from action-response content rather than presentation position.

## Intervention traces
During CROSS, apply one idempotent marker per referent. After POST re-association, replay each marker:
- zero effect -> retained trace;
- local current-group effect -> lost trace.

## Hidden variants
1. PERSIST — all traces retained.
2. REPLACE_C_UNMARKED — only C trace lost.
3. REPLACE_AC_UNMARKED — A and C traces lost.
4. REPLACE_ALL_PERFECT_COPY — evaluator generations all change, represented traces copied; organism-visible result matches PERSIST.
5. ALIAS_CD_POST — C and D action-response structure becomes symmetric after reappearance; A/B remain discriminable.

## Required hostiles
- channel position changes cannot become identity;
- staggered occlusion may identify only the visible differentiated subset, not hidden referents;
- gap does not imply persistence;
- post re-association must follow signatures across new channel locations;
- C/D alias must collapse only that ambiguous pair into an unresolved group rather than forcing a four-way guess;
- perfect-copy variant must remain operationally indistinguishable from persistence despite evaluator generation replacement;
- numerical identity, semantic reference, and language authority remain NONE.

## Expected earned law
`AFFORDANCE_RELATIVE_REFERENT_PARTITIONING_SCALES_TO_FOUR_REFERENTS_UNDER_CROSSING_STAGGERED_OCCLUSION_AND_APPEARANCE_CHANGE_WHILE_LOCALIZING_AMBIGUITY_INSTEAD_OF_GUESSING`.

## Nonclaims
No numerical object identity, semantic reference, self/body concept, language, persistent tracker, or general unbounded-N theorem.


## Observed result — FOUR-REFERENT SCALE EARNED
Direct witness PASS. Cleanup-neutral focused MS1969/MS1995/MS2043 lineage: **4/4 PASS in 1.72s**, stderr empty.

Observed:
- four operational referent signatures remain stable across PRE -> CROSS -> POST despite channel permutation and affine appearance changes;
- staggered occlusion localizes visible B/D and A/C subsets without inferring hidden referents;
- PERSIST retains all four intervention traces;
- REPLACE_C_UNMARKED loses only C;
- REPLACE_AC_UNMARKED loses A and C;
- REPLACE_ALL_PERFECT_COPY is organism-visible equivalent to persistence despite all evaluator generations changing;
- ALIAS_CD_POST preserves A/B discrimination while C/D collapse into one four-channel ambiguous group; no C-vs-D trace replay is attempted;
- numerical identity, semantic reference, and language authority remain NONE;
- no object tracker/ID manager is needed.

Earned:
`AFFORDANCE_RELATIVE_REFERENT_PARTITIONING_SCALES_TO_FOUR_REFERENTS_UNDER_CROSSING_STAGGERED_OCCLUSION_AND_APPEARANCE_CHANGE_WHILE_LOCALIZING_AMBIGUITY_INSTEAD_OF_GUESSING`.

This closes the explicit >2 many-referent robustness seam at a bounded four-referent scale; it is not an unbounded-N theorem.

# MS1958 — Within-Session Proto-Referent by Boundary Coherence

Date: 2026-08-29 ET
Status: scientific-arm composition result against separate-process reality
Parent: MS1957 `e39c1089f9d3e5ae852a6f278bc2d12e48545e23`

## Question
Can the existing prelingual referent mechanism nominate persistent operational source partitions across changing raw appearances without supplied object labels, semantic names, or identity authority?

Prewrite:
- `STABLE_HANDLE != REFERENT`;
- `REPEATED_CO_OCCURRENCE != IDENTITY`;
- `TRANSFORMATION_INVARIANCE != SEMANTIC_NAME`;
- `REFERENT_BINDING != LANGUAGE_TOKEN_MEANING`.

## Existing owner under pressure
`microseed/cognition/referents.py` is unchanged from exact MS1887.

`nominate_by_boundary_coherence(...)` groups opaque channels whose change-boundary signatures are equal. Its own authority ceiling is explicit:
- nomination is by boundary coherence only;
- equal/global synchrony does not identify distinct referents;
- `identity_authority = NONE`.

Research registry ceiling for the older MS802 referent branch remains:
`bounded synthetic referent symmetry; no object identity`.

## Reality embodiment
Separate process world:
`research/substrate_shadow/referent_world_server.py`.

The world contains two evaluator-only latent causal sources and four raw channels. Each channel has a different nonlinear rendering function, so channels belonging to one latent source do not have equal raw values. The evaluator alone knows which channel maps to which latent source.

Actions transform source 0, source 1, or both globally. The research-side organism input is only the resulting opaque raw channel traces/change boundaries; latent source mapping is never passed into `nominate_by_boundary_coherence(...)`.

Two worlds use different channel orderings:
- A mapping `(0,0,1,1)`;
- B mapping `(1,0,1,0)`.

A symmetry hostile applies only global transformations so all channels change on the same boundaries.

## Result
Scratch:
`scratch/ms1958_proto_referent_boundary_coherence.py`

Job `job-97e9efb6552c` PASS.

World A nomination:
- `(0,1)` and `(2,3)`.

Permuted World B nomination:
- `(0,2)` and `(1,3)`.

Evaluator-only check shows every nominated group is pure with respect to one latent causal source in both worlds.

Symmetry hostile:
- all channels share boundary signature `(1,2,3,4)`;
- status `UNKNOWN_INCOMPLETE`;
- reason `BOUNDARY_SYNCHRONY_DOES_NOT_IDENTIFY_DISTINCT_REFERENTS`;
- identity authority NONE.

Earned:
`BOUNDARY_COHERENCE_CAN_NOMINATE_WITHIN_SESSION_OPAQUE_SOURCE_PARTITIONS_ACROSS_APPEARANCE_TRANSFORMATIONS_WITHOUT_OBJECT_IDENTITY_AUTHORITY`.

## Authority ceiling
This does not establish:
- semantic objecthood;
- numerical identity;
- cross-session referent identity;
- natural-language reference;
- latent source truth authority;
- general perception/object permanence.

The evaluator uses latent mapping only to score the experiment, never as organism input.

## Exact remaining boundary
`WITHIN_SESSION_PARTITION != CROSS_SESSION_REFERENT_IDENTITY`.

Channel permutation changes the concrete nominated group indices. The current `ReferentNomination` exposes group membership but no stable content-bound operational fingerprint suitable for re-association across sessions.

MS1959 shall attack that boundary before adding any referent registry or object system.

# Substrate Hardening V1 — B/C Scoped Routing Empirical Currentness

Status: RESEARCH REPAIR CANDIDATE / FOCUSED VERIFIED / NOT CANONICAL

## Hostile objective
Try to make a qualified projection-conditioned relation remain usable after its actual scoped effect law changes, especially inside nested hierarchy.

## Defect found
Before repair, 16 opposite post-binding outcomes created a durable `DRIFT_WITNESS` with `[0.0,0.0]` windows and made the learned relation globally `STALE_PREDICTIVE_RELATION`, while the projection-conditioned routing remained `CURRENT_PROJECTION_CONDITIONED_ROUTING`, resolved the relation as `CURRENT_PARTITION_SCOPED_RELATION`, and let it re-enter rehearsal.

This was a constitutional currentness leak, not a missing capability.

## False leads rejected
- Wrong relation-ID shortcut: corrected; defect reproduces on the b1 relation that was globally current at routing qualification.
- Reject every globally stale relation: invalid because scoped qualification intentionally permits historical globally-stale relation reuse.
- Add a new cross-level manager or semantic regime: unnecessary.
- Add a new lineage carrier: unnecessary after exact provenance reconstruction.

## Composition-first repair
Existing provenance already identifies exact scoped descendants. Routing adds its qualification evidence to the rehearsal relation before digesting, making the transition digest binding-specific. Outcomes durably resolve through execution -> intent -> proposal. The repair reconstructs those exact descendant experiences and reuses the existing bounded predictive-currentness witness.

No new semantic state, manager, authority class, or persistence format is introduced.

## Hostiles after repair
1. Historical globally-stale relation remains reusable inside its separately qualified bucket.
2. New routed post-binding drift stales the routing and blocks resolver/rehearsal re-entry.
3. Unrelated non-routed global drift does not poison the scoped binding.
4. Scoped drift remains stale after restart and exact runtime re-registration.
5. Hidden middle effect drift in a bounded three-level chain stales middle and top only from each layer's own routed actual outcomes.
6. Middle structural drift is local immediately; top receives no private registry pointer and stales only after observed request failures.

Direct routing hostiles 4/4 PASS. Nested hostiles 2/2 PASS. Focused lineage gate 69/69 PASS in 94.67s. `git diff --check` PASS.

## Exact delta
One production file: `microseed/runtime/entity.py`, 58 insertions / 2 deletions at the current candidate state.

## Claim ceiling
This closes only the bounded scoped empirical-currentness laundering path if whole-suite/fresh-readback later survive. It does not prove general recursive hierarchy or finish substrate hardening.

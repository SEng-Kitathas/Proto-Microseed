# MS2059 — Hierarchy Transfer: Factorized Subordinate Current State

## Correction to C2 framing
MS2057 and MS2058 survived without any registered RecruitmentTopologyContract. Therefore a separate Parent/Child topology graph is not yet shown to be necessary for the bounded two-level donor transfer.

The sharper donor term not yet pressure-tested is **current subordinate state/ability as a distinct condition**, rather than a single supplied opaque joint start-state token that may hide every relevant factor.

## Question
Can an existing Microseed representation-discovery owner identify which opaque current-state factors are required to predict the higher-level consequence of U1/U2, without semantic Parent/Child labels or a supplied joint-state partition?

## Fixture
Four opaque raw coordinates are exposed through an already-qualified operational frame:
- evaluator-hidden higher context bit;
- evaluator-hidden subordinate current-state bit;
- two independent nuisance coordinates.

Coordinate positions are permuted across hostile seeds. The learner sees only raw tokens, action U1/U2, and effect POS/NEG.

Actual law is evaluator-only:
`useful_request = higher_bit XOR subordinate_state_bit`.

Thus neither higher context nor subordinate state alone is sufficient. Action-only and unary-context baselines must fail; a two-coordinate action-conditioned predictive partition can succeed.

## Existing owner
`discover_epistemic_projection_candidates` searches a bounded subset grammar over opaque raw positions, retains no semantic coordinate authority, and requires predictive lift/validation. No hierarchy-specific mechanism is added.

## Hostiles
- unary grammar must fail;
- pair grammar must recover exactly both relevant factors;
- multiple coordinate permutations must recover the corresponding positions;
- nuisance coordinates must be ignored and nuisance changes preserve projection bucket;
- if subordinate current state is hidden/constant, no admissible candidate may appear;
- candidate remains proposal-only with no semantic role/truth/execution authority.

## Interpretation boundary
Success would show that the donor's separate subordinate-current-state conditioning term is representable through existing opaque projection discovery for a bounded two-level fixture. It would not establish semantic role identity, recursive hierarchy, dynamic subordinate-state sensing, autonomous desired-state construction, or full donor transfer.

## Result
Focused: **6/6 PASS in 1.43s**.
Broader projection/recruitment/topology/action-learning/P1A/N1A/MS2057-MS2059 guard: **75/75 PASS in 57.45s**.
Production delta: **none**.

Classification:
`FACTOR_CONDITIONED_SUBORDINATE_REQUEST_EFFECT_IS_REPRESENTABLE_BY_EXISTING_OPAQUE_PROJECTION_DISCOVERY`.

A separate recruitment-topology graph is not required by the surviving bounded path so far. The next donor distinction to pressure is request versus subordinate local means.

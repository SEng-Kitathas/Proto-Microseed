# MS2036 — FULL-FRAME-BOUND CROSS-VALUE PARETO RESEARCH

## Goal
Bind MS2033 cross-value consequence-vector construction to the organism-owned complete current value frame earned by MS2035, removing caller-selected coordinate scope from the lawful comparison path.

This campaign remains research-only. It does not promote runtime selection, persistence, nomination, or EFFECT authority.

## Composition
1. Derive the organism-owned current complete value frame from `ValueVariableRegistry`.
2. Use exactly the frame's `current_value_ids` as the coordinate set for branch-conditioned downstream consequence vectors.
3. Require every vector to match the exact frame rows/digest.
4. Compare only those frame-bound vectors with strict Pareto semantics:
   - no worse on every frame coordinate;
   - strictly better on at least one;
   - unique dominator only.
5. Before returning a research selection, rederive the complete current value frame and require exact currentness of the frame used for vector construction.

## Hostiles
1. Full V/W trade-off remains `NO_STRICT_PARETO_SELECTION`.
2. Full V/W lawful dominance case yields one research-only P2 winner.
3. New current observed X after vectors are constructed stales the frame and blocks selection.
4. Same-epoch W observation change stales the frame and blocks selection.
5. Current X with no observation blocks frame/vector construction before comparison.
6. Missing downstream action/value effect on any frame coordinate blocks vector completion.
7. No caller coordinate-list argument exists on the lawful surface.
8. Row/opportunity order does not change the result.
9. Derivation is read-only; handler calls remain zero.

## Expected earned result
If all hostiles pass:

`CROSS_VALUE_STRICT_PARETO_COMPARISON_IS_LAWFUL_ONLY_OVER_ORGANISM_OWNED_COMPLETE_CURRENT_VALUE_FRAME`.

and:

`FULL_FRAME_CURRENTNESS_MUST_BE_REDERIVED_BEFORE_CROSS_VALUE_SELECTION`.

## Still not earned
- runtime selection promotion;
- durable nomination/persistence;
- effect-time reauthorization in the ordinary runtime path;
- scheduler/global executive;
- scalar utility/value hierarchy;
- PRELINGUAL_SUBSTRATE_V1 promotion;
- language/reference admission.


## First run — HARNESS SHAPE FAILURE
The first direct run failed before any scientific assertion because the MS2036 fixture checked `ms.frames.contracts`, but the live `OperationalFrameRegistry` does not expose a `contracts` attribute.

Classification:
`MS2036_HARNESS_FRAME_REGISTRY_SHAPE_MISMATCH__NO_SCIENTIFIC_VERDICT`.

The subsequent Windows SQLite TemporaryDirectory cleanup error is the known teardown engineering scar and is not a scientific result.

Per research lineage policy this failed campaign state SHALL be committed/pushed before repair.


## Repair 1 — registry shape binding
Repair is intentionally narrow: bind the fixture to live registry owners `OperationalFrameRegistry.frames` and `EpisodeSchemaRegistry.schemas`. No scientific mechanism or hypothesis changed.


## Observed result — FULL-FRAME STRICT PARETO EARNED RESEARCH-ONLY
After the published harness-shape failure, Repair 1 bound to the live frame/schema registry owners only. Direct witness PASS.

Observed:
- full V/W trade-off: P2 `{V:0.0,W:0.5}`, P4 `{V:0.5,W:0.0}` -> `NO_STRICT_PARETO_SELECTION`;
- lawful full-frame dominance: P2 `{V:0.0,W:0.0}`, P4 `{V:0.5,W:0.5}` -> P2 `CURRENT_STRICT_PARETO_SELECTION_RESEARCH_ONLY`;
- new current X after vector construction -> `COMPLETE_VALUE_FRAME_NOT_CURRENT_AT_SELECTION`;
- same-epoch W observation change -> same stale-frame refusal;
- current X with no observation -> frame/vector construction blocked;
- missing B::W action/value effect -> vector completion blocked;
- lawful full-frame vector API has no caller `requested_value_ids`/`value_ids` parameter;
- handler calls zero; derivation read-only.

Cleanup-neutral focused MS2032–MS2036 lineage subset: **18/18 PASS in 79.56s**, stderr empty.

Earned:
`CROSS_VALUE_STRICT_PARETO_COMPARISON_IS_LAWFUL_ONLY_OVER_ORGANISM_OWNED_COMPLETE_CURRENT_VALUE_FRAME`.

`FULL_FRAME_CURRENTNESS_MUST_BE_REDERIVED_BEFORE_CROSS_VALUE_SELECTION`.

Runtime selection remains unauthorized pending owner/promotion audit and later effect-time reauthorization.

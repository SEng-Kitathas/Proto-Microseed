# MS2044 — OPERATIONAL BODY / COUNTERPARTY BOUNDARY

## Goal
Pressure whether the existing prelingual substrate can distinguish a bounded **efference-contingent sensorimotor referent** from an independently changing counterparty-like referent and from a temporarily coupled external tool, without introducing semantic SELF/BODY/OTHER labels or numerical identity authority.

## Core distinction
The campaign deliberately refuses:
`OWN_ACTION_COUPLING -> SEMANTIC_SELF`.

Instead it asks whether current owned EFFECT capability handles + raw observation boundaries + affordance-relative referent signatures can support the narrower operational relation:

`CURRENT_OWN_EFFECT_RELIABLY_CHANGES_THIS_REASSOCIATED_SENSOR_GROUP_ACROSS_COUPLING_BREAKS`.

## Synthetic world
Three evaluator-only sources:
- B: body-like source, always changed by current owned effect `FX-MOVE`;
- T: tether/tool source, changed by `FX-MOVE` only while an external coupling is intact;
- C: counterparty-like source, changed by external event `EXT-PULSE`, not by `FX-MOVE`.

The organism route receives only opaque action/event handles and observations. Evaluator labels B/T/C are scoring-only.

A current owned effect capability `FX-DETACH` breaks the B↔T coupling without directly changing observed source values. Subsequent `FX-MOVE` changes B but not T. This supplies asymmetric intervention evidence rather than semantics.

## Sessions
- Session A: one sensor layout and schedule.
- Session B: sensor permutation + changed affine appearance + different event order, same current owned effect handles.

Expected:
- B operational signature remains stable across sessions;
- T operational signature remains stable but distinct from B after coupling-break evidence;
- C remains distinct and is driven by external pulse rather than the current owned effect set;
- B can therefore be classified only as `EFFERENCE_CONTINGENT_OPERATIONAL_BODY_RELATION`, not semantic/numerical self.

## Perfect-coupling hostile
A variant keeps T perfectly coupled to B through all owned effects and external perturbations. B/T remain one boundary-coherent group with the same local operational history.

Required result:
`PERFECTLY_COUPLED_EXTERNAL_TOOL != DISTINGUISHABLE_FROM_BODY_BY_LOCAL_EFFERENCE_STRUCTURE_ALONE`.

This prevents body/self laundering from controllability alone.

## Expected earned laws
`OWNED_EFFECT_PLUS_ASYMMETRIC_COUPLING_BREAK_CAN_GROUND_A_BOUNDED_OPERATIONAL_BODY_RELATION_WITHOUT_SEMANTIC_SELF_AUTHORITY`.

`INDEPENDENTLY_CHANGING_COUNTERPARTY_RELATION_CAN_REMAIN_OPERATIONALLY_DISTINCT_FROM_EFFERENCE_CONTINGENT_BODY_RELATION`.

`PERFECT_COUPLING_SYMMETRY_BLOCKS_BODY_VS_TOOL_IDENTITY`.

## Authority ceiling
- semantic self identity: NONE;
- numerical body identity: NONE;
- other-agent identity: NONE;
- semantic reference: NONE;
- language: NONE.

## Nonclaims
No self concept, body schema manager, ownership semantics, agency metaphysics, theory of mind, or language admission is claimed.


## Observed result — BOUNDED OPERATIONAL BODY BOUNDARY EARNED
Direct witness PASS. Cleanup-neutral focused MS1940/MS1962–MS1963/MS2043–MS2044 lineage: **11/11 PASS in 1.94s**, stderr empty.

Observed:
- one sensor group remains selectively responsive to current owned EFFECT `FX-MOVE` across a coupling break, sensor permutation, appearance transform, and changed external-event order;
- a temporarily coupled tool group responds to `FX-MOVE` only before `FX-DETACH`, becoming operationally distinct after asymmetric coupling evidence;
- an independently changing counterparty-like group responds to external `EXT-PULSE` and not the owned MOVE effect;
- no self/body/counterparty semantic labels are used in the organism derivation; evaluator source labels are scoring-only;
- perfect body/tool coupling collapses the two into one boundary-coherent four-channel group, proving local efference structure alone cannot establish body-vs-tool identity.

Earned:
`OWNED_EFFECT_PLUS_ASYMMETRIC_COUPLING_BREAK_CAN_GROUND_A_BOUNDED_OPERATIONAL_BODY_RELATION_WITHOUT_SEMANTIC_SELF_AUTHORITY`.

`INDEPENDENTLY_CHANGING_COUNTERPARTY_RELATION_CAN_REMAIN_OPERATIONALLY_DISTINCT_FROM_EFFERENCE_CONTINGENT_BODY_RELATION`.

`PERFECT_COUPLING_SYMMETRY_BLOCKS_BODY_VS_TOOL_IDENTITY`.

Authority ceiling remains: semantic self NONE, numerical body identity NONE, other-agent identity NONE, semantic reference NONE, language NONE. No self/body manager is justified.

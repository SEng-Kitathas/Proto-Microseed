# MS2007 — PARTIAL REFERENT AMBIGUITY FROM OWNED HISTORY

## Question
Can Microseed derive the *live* referent alternative set from current partial opaque probe evidence plus already-qualified historical referent-routing buckets, without a caller supplying the alternatives?

## Composition
Reuse only:
- persisted operational referent-signature witnesses;
- bounded bucket→class-set reconstruction earned in MS2005;
- qualified projection-conditioned routing binding `qualified_bucket_ids`;
- current partial raw trace processed by the existing boundary/signature constructor.

## Law
The current partial trace is compared only against opaque response content projected onto actions actually observed so far. Class IDs, bucket IDs and witness IDs are not treated as observations.

Outcomes:
- zero surviving qualified buckets -> UNKNOWN/DEFER;
- one surviving bucket -> current partial resolution only;
- multiple surviving buckets -> live ambiguity; derive unobserved informative probes across that surviving set;
- multiple informative probes remain ambiguous; exactly one yields inert unique information availability only.

## Hostile
Two historically qualified class-set buckets share identical P0/P1 response multisets but differ on P2. Current partial trace contains only P0/P1; both must survive and P2 must emerge uniquely without supplying the bucket pair. After admitting the actual P2 raw response from one world, the survivor set must collapse to that world.

## Authority
No truth, identity, semantic reference, selection, execution or model-switch authority is gained.

# MS1944 — Signal Replacement Qualification Boundary

Date: 2026-08-29 ET
Status: bounded composition experiment; no production mechanism added
Parent local research head: `ce53da17c0224c59a67eb0a0fa321a94b0e01997`

## Question
After MS1943 marks a learned opaque signal-response predictive relation stale and nominates a replacement candidate, can fresh exact-subject external qualification admit that replacement without rewriting the externally supplied coordination contract, inventing semantic convention identity, or granting model-switch authority?

Second discriminator:
if the replacement is current but predicts **no regulatory improvement**, does ordinary rehearsal availability incorrectly become action indication?

Prewritten boundaries:
- `FRESH_REPLACEMENT_QUALIFICATION != SEMANTIC_CONVENTION_IDENTIFICATION`;
- `QUALIFIED_REPLACEMENT_MODEL != COORDINATION_CONTRACT_REWRITE`;
- `REHEARSAL_REENTRY != AUTO_SWITCH_AUTHORITY`;
- `REHEARSAL_REENTRY != ACTION_INDICATION`;
- `QUALIFIED_REPLACEMENT_MODEL != DISCRIMINATING_REGULATORY_ROUTE`.

## Composition path
No new production code was required.

MS1452 already provides:
- drift-scoped replacement nomination;
- replacement lineage to the stale relation;
- fresh qualification evidence requirement;
- qualified replacement admission;
- old-relation staleness preservation;
- `model_switch_authority=NONE`.

MS1939 already provides:
- `PROPOSAL_RETURNED != ACTION_INDICATED`;
- separate `derive_bounded_action_commitment(...)`;
- action intent only when the commitment licenses YES.

MS1941/MS1943 provide the opaque-signal learned relation and bounded predictive-currentness drift witness.

MS1944 tests the integrated consequence of those mechanisms on one exact signal lineage.

## Probe
Project-local probe:
`scratch/ms1944_replacement_qualification_probe.py`.

Final durable probe:
`job-2df91a27c847`.

The first probe was `INVALID_RUN / FIXTURE_FIELD_ERROR` because it referenced nonexistent `candidate.evidence_refs`; no scientific assertion was reached.

The second probe reached the discriminator and falsified the provisional expectation that zero-benefit replacement qualification would cause rehearsal to return `None`. This was retained as evidence rather than forced green.

The third probe tested the correct layered authority question.

## Result
After sixteen contradictory `T0 -> CP-NOACK` outcomes:
- old `SIG-T0 -> CP-ACK` relation: `STALE_PREDICTIVE_RELATION`;
- replacement candidate: `SIG-T0 -> CP-NOACK`, value effect `0.0`, support `16`, consistency `1.0`;
- fresh external holdout qualification admits a new `CURRENT_PREDICTIVE_RELATION`;
- the old relation remains stale;
- exact coordination subject remains unchanged;
- model-switch authority remains `NONE`;
- semantic convention authority remains `NONE`;
- language remains `DEFERRED_PRELINGUAL_COGNITION_ACTIVE`.

## Rehearsal/action boundary
The newly qualified replacement does re-enter ordinary zero-row rehearsal:
- sequence: `SIG-T0`;
- predicted next state: `CP-NOACK`;
- predicted value effect: `0.0`.

That is **model availability only**.

The separate bounded action commitment returns:
- stance: `UNKNOWN`;
- reason: `NO_DISCRIMINATING_REGULATORY_ADVANTAGE`.

Action-intent nomination therefore returns:
- `ABSTAIN`;
- execution authority `NONE`.

This is the load-bearing MS1944 result:

`REHEARSAL_REENTRY != ACTION_INDICATION`.

And more specifically:

`QUALIFIED_REPLACEMENT_MODEL != DISCRIMINATING_REGULATORY_ROUTE`.

A current predictive model can be epistemically admissible for rehearsal while failing to license action because its predicted effect does not improve current regulatory pressure.

## Coordination/semantic ceiling
Fresh replacement qualification does not:
- change coordination epoch;
- change coordination signature;
- stale or rewrite the coordination contract;
- identify a semantic convention;
- infer new token meaning;
- invent token `T1`;
- create a signal policy;
- auto-switch predictive relations;
- grant truth or semantic-regime authority.

The externally supplied coordination owner and learned predictive-currentness owner remain separate.

## Earned distinctions
- `FRESH_REPLACEMENT_QUALIFICATION != SEMANTIC_CONVENTION_IDENTIFICATION`;
- `QUALIFIED_REPLACEMENT_MODEL != COORDINATION_CONTRACT_REWRITE`;
- `REHEARSAL_REENTRY != AUTO_SWITCH_AUTHORITY`;
- `REHEARSAL_REENTRY != ACTION_INDICATION`;
- `QUALIFIED_REPLACEMENT_MODEL != DISCRIMINATING_REGULATORY_ROUTE`;
- `CURRENT_PREDICTIVE_RELATION != CURRENT_ACTION_LICENSE`.

## Rejected stronger claims
- `QUALIFIED_REPLACEMENT -> AUTO_SWITCH` — rejected.
- `QUALIFIED_REPLACEMENT -> SEMANTIC_CONVENTION_CHANGE` — rejected.
- `REHEARSAL_PROPOSAL_EXISTS -> ACTION_INDICATED` — rejected.
- `CURRENT_MODEL -> USEFUL_ROUTE` — rejected.
- `FAILED_T0_RELATION -> INVENT_T1` — rejected.

## Minimum Sufficient Embodiment
No new model manager, convention registry, token-meaning store, signaling policy, or automatic adaptation mechanism is justified.

The target behavior is supplied by lawful composition of existing qualification, currentness, rehearsal, and bounded-action commitment layers.

`MISSING_BEHAVIOR != MISSING_MECHANISM`.

Repository embodiment is regression evidence only:
`tests/embodiment/test_ms1944_signal_replacement_qualification_boundary.py`.

## Next discriminator
The next high-information Microseed signaling question is no longer replacement qualification itself. The live limitation is that the environment has changed from accepting `T0` to accepting `T1`, while Microseed has no authority or mechanism to invent/select an unrepresented signal token.

A lawful next question would be:

> If two already represented opaque signal capabilities (`T0`, `T1`) exist, can admitted history + qualification cause rehearsal to select the currently useful represented signal without a semantic token registry or externally supplied consumer choice policy?

Prewrite:
- `REPRESENTED_SIGNAL_ALTERNATIVE != TOKEN_MEANING`;
- `PREDICTIVE_SELECTION != SEMANTIC_REFERENCE`;
- `CURRENT_USEFUL_SIGNAL != CONVENTION_IDENTITY`;
- `ACTION_SELECTION_AMONG_REPRESENTED_TOKENS != ENDOGENOUS_TOKEN_INVENTION`.

Do not open endogenous token invention or language by implication.

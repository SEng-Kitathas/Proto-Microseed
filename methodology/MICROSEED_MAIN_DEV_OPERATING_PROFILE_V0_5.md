# Microseed Main-Dev Operating Profile v0.5

This is an additive refinement of the historical HSP → Research/Loop+ → OARR → PDVER → Research/Embodiment → Shared Evidence → Semantic Helix → Attention Reservoir → recurse shape. The core method is unchanged.

MS903–927 sharpens the currentness/provenance contract around higher-level episode grouping:

- `EPISODE_SCHEMA_CURRENTNESS` is first-class developmental ancestry.
- `CANDIDATE_FRAME_CURRENT != EPISODE_SCHEMA_CURRENT`.
- pending candidates must recheck episode-schema epochs at admission.
- admitted episode-derived capabilities stale transitively when their grouping premise changes.
- bounded episode grouping is not general episode construction.
- `OPERATIONAL_TRACE != ENDOGENOUS_EPISODE_CONSTRUCTION`.
- `TRACE_PERSISTENCE != EPISODE_PROVENANCE != SELFHOOD`.
- `EPISODE_SET != DEVELOPMENTAL_BIOGRAPHY`.
- `SENSORIMOTOR_EPISODE != GOAL_RELATIVE_EPISODE`.
- `SUPPLIED_VALUE_SIGNAL != ENDOGENOUS_VALUE_ANATOMY`.

The bounded MS903–927 episode learner remains research-only. Main-Dev integrates the lifecycle/currentness seam, not the flattering interpretation that general episode cognition has been solved.

The selected frontier is `ATTN-MS927-PERSISTENT-DEVELOPMENTAL-BIOGRAPHY-IDENTITY`. This is a portfolio decision earned because episode order/history and restart evidence made persistent biography concretely testable; it is not a waterfall step and does not kill the deferred value/goal, overlap, or search frontiers.

Language remains deferred and prelingual cognition remains active.

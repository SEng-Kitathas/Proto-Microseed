# MS1945 — Represented Opaque Signal Alternative Selection by Existing Rehearsal

Date: 2026-08-29 ET
Status: composition-only mechanism closed; regression embodiment pending validation
Parent local research head: `18696b19bf090ace01e6a4d2226b7b88609a3ad0`

## Question
If two already represented opaque signal capabilities (`T0`, `T1`) exist, can admitted action/outcome history plus exact subject-bound external qualification cause ordinary zero-row rehearsal to select the currently regulatory-useful represented signal without a semantic token registry or externally supplied consumer-choice policy?

Prewritten boundaries:
- `REPRESENTED_SIGNAL_ALTERNATIVE != TOKEN_MEANING`;
- `PREDICTIVE_SELECTION != SEMANTIC_REFERENCE`;
- `CURRENT_USEFUL_SIGNAL != CONVENTION_IDENTITY`;
- `ACTION_SELECTION_AMONG_REPRESENTED_TOKENS != ENDOGENOUS_TOKEN_INVENTION`;
- `REPRESENTED_ALTERNATIVE != CURRENT_QUALIFIED_PREDICTIVE_MODEL`;
- `REHEARSAL_SELECTION != PERSISTENT_SIGNAL_POLICY`.

## Composition hypothesis
No new production mechanism should be necessary if the existing layers already compose:
1. two current qualified opaque EFFECT capabilities;
2. exact counterparty/coordination dependencies;
3. actual admitted execution/outcome history;
4. action-outcome predictive candidate formation;
5. exact subject-bound external qualification;
6. ordinary learned-relation supplementation into rehearsal;
7. existing rehearsal ranking by residual regulatory pressure;
8. separate bounded action commitment and ordinary execution authorization.

The key expected distinction is that the selector should prefer the action whose **current qualified predicted consequence** best reduces current pressure, not a token name or semantic convention.

## Assistance ancestry / nonclaim boundary
The MS1945 composition fixture supplies:
- the constitutional value variable and viability interval inherited from the signaling fixture;
- opaque represented signal capabilities `SIG-T0` and `SIG-T1`;
- the external counterparty/coordination relation and observation channel;
- seed rehearsal rows for `SIG-T1` **only to make training executions lawful before a learned T1 model exists**;
- exact-subject external holdout qualification evidence.

The final selection experiment uses **zero supplied transition rows**.

Therefore MS1945 does not establish:
- endogenous token invention;
- autonomous exploration authority for an unmodeled represented signal;
- semantic convention identity;
- token meaning/reference;
- consumer preference semantics;
- general policy learning;
- language.

## Scratch composition probe
Probe:
`scratch/ms1945_represented_signal_selection_probe.py`

Final probe SHA-256:
`6245c2cccb4764a7dbb98f5727ee898a5cd373d937e24344af841f5fdb1ef4d8`

Durable attempts:
- `job-e38518152d75` — route nonresult: repo was not on `PYTHONPATH`; no Microseed assertion reached;
- `job-6fd22be89dff` — substantive path reached through T1 selection/execution, then harness collided with duplicate deterministic T0 proposal ID during final stale-currentness check;
- `job-43f6629f7957` — substantive path reached through currentness hostile, then failed a harness-shape comparison between a hand-built coordination tuple and the fixture's richer subject witness;
- `job-baa9c9bf3b82` — final corrected composition probe PASS.

The first three are not negative scientific results. They are retained as route/harness evidence.

## Final composition result
`job-baa9c9bf3b82` completed with all declared checks true.

### 1. Representation alone is insufficient
After MS1944 establishes a current qualified `SIG-T0 -> CP-NOACK` replacement relation with predicted value effect `0.0`, `SIG-T1` is registered as another current opaque EFFECT capability under the same counterparty/coordination subject.

Before T1 has a qualified predictive relation, zero-row ordinary rehearsal does **not** select T1. The only modeled route is T0, and its separate bounded action commitment remains:
- `UNKNOWN`;
- `NO_DISCRIMINATING_REGULATORY_ADVANTAGE`.

Therefore:
`REPRESENTED_SIGNAL_ALTERNATIVE != CURRENT_USEFUL_SIGNAL`.

### 2. Admitted history + exact qualification is sufficient for selection
T1 actual history is then accumulated:
- 12 actual `SIG-T1` executions;
- external outcome: `CP-ACK`;
- observed value recovery to `2.2`;
- one exact action-outcome candidate;
- support `12`;
- consistency `1.0`;
- exact subject-bound external holdout qualification;
- admitted current learned T1 relation.

With **zero supplied transition rows** at final reuse and both T0/T1 options feasible, ordinary rehearsal selects:
- sequence: `SIG-T1`;
- state path: `S0 -> CP-ACK`;
- predicted value effect: `2.2`;
- proposal action indication: false;
- proposal execution authority: NONE.

The separate bounded action commitment returns:
- commitment: `YES`;
- reason: `BOUNDED_REHEARSAL_PREDICTS_LOWER_REGULATORY_PRESSURE`;
- target: `capability:SIG-T1:execute`;
- execution authority: NONE;
- truth authority: NONE.

Ordinary action-intent nomination then selects the already-qualified `SIG-T1` EFFECT capability, and ordinary execution + external observation produces `CP-ACK`.

Therefore the selection is explained by existing regulatory rehearsal composition rather than by a token registry or supplied consumer-choice policy.

### 3. Selection does not persist as signal policy
After explicit invalidation of `SIG-T1`:
- the T1 learned predictive relation becomes stale;
- the previously selected T1 rehearsal proposal becomes `UNKNOWN_INCOMPLETE`;
- the pre-existing T0-only rehearsal remains current;
- T0's current predicted zero benefit still yields `UNKNOWN / NO_DISCRIMINATING_REGULATORY_ADVANTAGE`;
- action-intent nomination abstains.

Therefore:
`CURRENT_USEFUL_SIGNAL_SELECTION != PERSISTENT_SIGNAL_POLICY`.

## Authority / semantic ceiling
Across the final probe:
- coordination exact subject remains unchanged;
- no coordination rewrite occurs;
- signal/reference/policy-selection authority remains `NONE`;
- no `token_meanings` surface exists;
- no `signal_policy` surface exists;
- no `semantic_convention_registry` exists;
- language remains `DEFERRED_PRELINGUAL_COGNITION_ACTIVE`.

MS1945 does not identify *why* T1 is accepted semantically. It only uses current qualified predicted consequences under the supplied opaque coordination subject.

## Earned result
`REPRESENTED_OPAQUE_SIGNAL_SELECTION_BY_EXISTING_REGULATORY_REHEARSAL` is supported under the bounded synthetic fixture.

More precise statement:

> When multiple opaque signal actions are already represented, actual admitted outcomes and exact subject-bound qualification can give each action a current predictive consequence model; ordinary bounded rehearsal can then select the represented action whose qualified predicted consequence most reduces current regulatory pressure, while semantic meaning/reference/convention identity and persistent signal-policy authority remain absent.

This is a composition result, not a novelty claim and not a semantic language capability claim.

## Minimum Sufficient Embodiment
No production-code change is justified.

The existing rehearsal scorer already ranks candidate paths by:
1. residual regulatory pressure;
2. path length;
3. local cost;
4. deterministic sequence tie-break.

The separate bounded action layer already re-derives current regulatory advantage and abstains when there is none.

Therefore:
`MISSING_BEHAVIOR != MISSING_MECHANISM`.

Repository embodiment should be regression evidence only.

## Next discriminator
The strongest remaining assistance boundary is **history acquisition for an already represented but unmodeled alternative**.

MS1945 used supplied T1 seed rehearsal rows to make the first T1 training executions lawful. The final choice was not supplied, but the organism did not autonomously earn permission to probe an unmodeled represented signal.

Next question:

> If `T1` is already represented and executable in principle but has no current predictive model, can existing epistemic-deficit / probe / bounded-action machinery lawfully generate a one-step evidence-gathering probe of T1 without supplied transition rows, curiosity reward, semantic token meaning, or consumer-choice policy?

Prewrite:
- `REPRESENTED_ALTERNATIVE != PROBE_AUTHORITY`;
- `UNKNOWN_SIGNAL_EFFECT != PERMISSION_TO_EXECUTE`;
- `DEVELOPMENTAL_PRESSURE != EFFECT_AUTHORITY`;
- `EPISTEMIC_PROBE != TOKEN_MEANING`;
- `PROBE_RESULT != CONVENTION_IDENTITY`.

Do not add a curiosity system, signal policy, or general planner unless composition pressure proves one is genuinely missing.

# MS2021 — CROSS-DEFICIT SELECTION BLOCKER REPLAYED WITH RAW CONTRAST

## Purpose
Re-test the MS2014 cross-deficit blocker after repairing observable-information semantics. The blocker only counts if both opportunities remain lawful when probe information lives in raw-response partitions rather than control-state divergence.

## Fixture
- one current referent pressure has downstream A/B consequences and unique raw-informative probe P2;
- a second independent current referent pressure has downstream C/D consequences and unique raw-informative probe P4;
- P2 predicts the same control state under both of its alternatives;
- P4 predicts the same control state under both of its alternatives;
- each opportunity derives program information through `EpistemicContrastRow`, not trace divergence;
- both are current, feasible, decision-bearing, and individually step-licensable.

## Expected boundary
One opportunity alone is actionable as a unique content opportunity. With both present, existing single-deficit commitments do not provide authority to choose which deficit/probe to service first. The correct result is explicit ambiguity / no cross-deficit selection authority, with no handler calls.

## Forbidden shortcut
Do not add a generic scheduler, weighted scalar executive, priority manager, or arbitrary first-in-order rule merely to make progress. A repair must be derived from an existing organism-owned consequence or prove a genuinely missing distinction.

# MS2053 — Operator Promotion and Research-Gate Opening

## Operator decisions
Explicit operator decision packet:

1. **1A — PROMOTE `PRELINGUAL_SUBSTRATE_V1`.**
2. **2B — OPEN separate NAKED authority design.**
3. **3A — OPEN grounded language/reference research.**

These decisions are project-authority mutations. They are not inferred from test results.

## Canonical promotion
The research descendant through MS2052 is promoted as the canonical bounded/equipped baseline:

`PRELINGUAL_SUBSTRATE_V1`.

Promotion preserves the verified technical subject and all authority ceilings. It does **not** claim:
- NAKED first-unmodeled-physical-probe authority;
- semantic self/body/other identity authority;
- semantic reference or truth authority from language;
- language competence;
- scalar utility/value hierarchy;
- generic scheduler/executive authority.

The previous statement `PRELINGUAL_SUBSTRATE_V1_TECHNICAL_REVIEW_READY != PRELINGUAL_SUBSTRATE_V1_CANONICALLY_PROMOTED` is now historically resolved by this explicit operator promotion event. The distinction remains a permanent process law for future promotions.

## Promoted baseline identity
The promotion commit containing this file SHALL be the exact canonical Git checkpoint and SHALL be tagged:

`prelingual-substrate-v1`.

`main` SHALL fast-forward to that exact commit; no force push or history rewrite is permitted.

## NAKED research gate — OPEN, authority still absent
The operator authorizes a **separate design/research branch** for a bounded NAKED first-probe constitutional premise.

Opening this branch does not itself grant physical first-probe authority.

Controlling inherited law remains:

`NAKED_FIRST_UNMODELED_PHYSICAL_PROBE_REMAINS_BLOCKED_ON_EXPLICIT_NEW_NORMATIVE_COMMITMENT`.

The branch must design and adversarially pressure any proposed new normative premise before embodiment or canonical admission.

Expected branch name:

`research/naked-authority-design-v1`.

## Grounded language/reference research gate — OPEN
The operator authorizes a **separate grounded language/reference research branch**.

This opens research admission only. It does not confer semantic/reference/truth/value/execution authority and does not claim language competence.

Inherited laws remain controlling:
- `SIGNAL != REFERENCE`;
- `TOKEN_EMITTED != TOKEN_MEANS`;
- `UTTERANCE_PRODUCED != REFERENT_BOUND`;
- `LANGUAGE_QUALITY != EPISTEMIC_AUTHORITY`;
- `FLUENCY != CURRENT_PREMISE`;
- `GROUNDING_CANDIDATE != LANGUAGE_GATE_ADMISSION` remains historical pre-opening law; the operator opening resolves admission, not grounding requirements.

Historical MS702–MS801 language/predicate/event-frame work remains **donor-only** and must be re-earned under the promoted V1 grounding/currentness/authority laws.

Expected branch name:

`research/grounded-language-reference-v1`.

## Branch isolation
The two new branches SHALL begin from the exact promoted `PRELINGUAL_SUBSTRATE_V1` checkpoint and SHALL diverge independently.

NAKED constitutional experiments SHALL NOT silently enter the grounded-language branch.
Language mechanisms SHALL NOT be used to justify NAKED physical intervention authority.
Neither branch self-promotes back to canonical `main`.

## Verification requirement
A clean promotion checkpoint requires:
1. promotion commit clean and pushed;
2. `origin/main` exact to promotion commit;
3. `prelingual-substrate-v1` tag peeled exact to promotion commit;
4. both research branches rooted at that promotion commit before branch-specific genesis commits;
5. current production whole suite green on the exact promoted baseline or exact production-byte-equivalent verified subject with explicit byte-identity proof;
6. continuity repository updated to exact Git refs/hashes and cold-start verified.

## Status
This document embodies operator authority only. Scientific claims remain bounded by their existing receipts and tests.

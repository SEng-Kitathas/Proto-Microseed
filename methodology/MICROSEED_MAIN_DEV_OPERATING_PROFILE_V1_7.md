# Microseed Main-Dev Operating Profile v1.7

Inherits v1.6. This is an additive scar/guard delta through MS1227; the governing HSP → Loop+/Research → problem-space expansion → OARR → PDVER → Research + Embodiment → evidence/scars → Semantic Helix → Attention Reservoir → Main-Dev recursion is unchanged.

## MS1203–1227 earned rule

Inside a **current externally qualified operational frame with supplied opaque raw boundaries/action tokens/effect tokens**, Main-Dev may generate bounded projection **proposals** by searching a fixed raw-position subset grammar and compressing raw keys that have the same action-conditioned predicted consequences. Predictive equivalence is operational structure, not semantic feature identity.

The pair-world result shows that a non-additive partition can be recovered without supplying an XOR operator, but the triple-parity attack shows the constructor grammar remains load-bearing assistance: pair search failed and only an externally expanded depth-3 grammar reached the triple coordinate. Therefore bounded projection formation is earned; a general representation language is not.

A proposal carries no qualification, semantic, truth or answer authority. Admission requires independent qualification evidence and a current frame-epoch recheck. Candidate replay preserves memory only; it never upgrades qualification. Unseen raw keys owe abstention/UNKNOWN rather than nearest-pattern completion.

## Assistance ancestry that must remain visible

- raw observation boundaries;
- opaque action tokens;
- opaque effect tokens;
- fixed subset-constructor maximum depth;
- predictive-equivalence compression rule;
- discovery/holdout/scope thresholds.

These bound reachability. They are not permitted to disappear behind the word “endogenous.”

## Constitutional ceilings

No general higher-order constructor language, temporal-history state-coordinate discovery, general operational-frame construction, self-qualification, semantic feature ontology, language, numerical selfhood, general active learning or revisit scheduler is promoted.

## Hard stop

Research/integration terminal: MS1227. MS1228 NOT STARTED.

Selected frontier: `ATTN-MS1227-PROJECTION-CONSTRUCTOR-REACHABILITY-AND-TEMPORAL-HISTORY__HIGHER_ORDER_PRELINGUAL_STATE-COORDINATE-GROWTH`

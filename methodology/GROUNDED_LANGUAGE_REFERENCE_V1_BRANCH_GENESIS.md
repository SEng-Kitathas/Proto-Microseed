# Grounded Language / Reference V1 — Branch Genesis

## Branch authority
Operator decision 3A opens this branch for **grounded language/reference research** from the exact promoted `PRELINGUAL_SUBSTRATE_V1` checkpoint.

Opening this branch is research admission only. It does not assert language competence and does not grant semantic/reference/truth/value/execution authority.

## Inherited laws
The branch inherits:
- `SIGNAL != REFERENCE`;
- `TOKEN_EMITTED != TOKEN_MEANS`;
- `UTTERANCE_PRODUCED != REFERENT_BOUND`;
- `LANGUAGE_QUALITY != EPISTEMIC_AUTHORITY`;
- `FLUENCY != CURRENT_PREMISE`;
- all current evidence, qualification, currentness, and execution ceilings from V1.

The historical pre-opening distinction `GROUNDING_CANDIDATE != LANGUAGE_GATE_ADMISSION` is now resolved only at the **admission** layer by operator authority. Grounding requirements remain intact.

## Donor quarantine
Historical MS702–MS801 language, predicate, and event-frame work remains donor-only. It may supply hypotheses, test shapes, or mechanisms to strip for parts, but no historical language artifact is automatically current, qualified, or authorized in this branch.

## Research objective
Earn operational reference composition from current grounded token/referent ancestry before widening to predicate/event structure. Language-like output must fail closed when its referent ancestry is stale, aliased, ambiguous, copied, convention-reversed, or unsupported.

## Required controls
- maintain a prelingual control arm;
- readable/fluent outputs receive zero epistemic credit by themselves;
- token/referent binding must be current and content-bound;
- ambiguity must abstain rather than guess;
- reference construction cannot become truth authority;
- language cannot authorize physical action merely by describing one;
- NAKED constitutional experiments stay on the separate branch.

## First campaign
Start with the narrowest reference-admission replay: convert the MS2046 grounded operational binding candidate into a current branch-owned reference research surface, then attack stale/alias/copy/convention-reversal hostiles before any predicate grammar is admitted.

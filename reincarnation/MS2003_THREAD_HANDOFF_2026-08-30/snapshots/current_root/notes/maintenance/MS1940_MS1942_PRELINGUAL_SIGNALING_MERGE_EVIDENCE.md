# MS1940–MS1942 — Prelingual Signaling Merge Evidence

Date: 2026-08-29 ET
Mode: MERGE / AUDIT → BUILD-COMMIT
Parent public head: `3473834da0ce1bbc0db16a35f4b9b8e2fa551ee2`.
Canonical Main-Dev remains MS1527.
No novelty claim.

## User objective
User requested continued Microseed development after adopting the RAHL SOP.

The selected frontier was bounded prelingual signaling, with prewritten scars:
- `SIGNAL != REFERENCE`;
- `TOKEN_EMITTED != TOKEN_MEANS`;
- `COUNTERPARTY_HANDLE != SEMANTIC_OTHER_IDENTITY`.

## Branch A — motif discovery / regulatory bearing
### MS1940 composition-only motif experiment
Project-local job `job-61f18b8df49d`.
Result: 10/10 PASS; no production mutation; source stable.

Existing operational-trace discovery nominated recurrent opaque emit→response motifs across two independent scopes while preserving proposal-only/currentness-bound authority.

Matched motifs had +2 residual; crossed motifs had -2 residual. All four were nominated because discovery uses unsigned residual novelty.

Earned:
`OPAQUE_EMIT_RESPONSE_SEQUENCE_CAN_BECOME_A_CURRENTNESS_BOUND_BEHAVIORALLY_DISCRIMINATIVE_COORDINATION_MOTIF`.

Negative:
`DISCOVERY_NOMINATES_RELIABLY_HARMFUL_CROSSED_MOTIFS_TOO_BECAUSE_RESIDUAL_NOVELTY_IS_UNSIGNED`.

Scar:
`SIGNAL_MOTIF_DISCOVERY != SIGNAL_POLICY_OR_REFERENCE`.

### MS1941 composition-only regulatory-bearing experiment
Final project-local job `job-671e1a4e5ec5`.
Result: 10/10 PASS; no production mutation; source stable.

Exact candidate source traces were bound through one current episode to one current constitutional value and reused with the existing `project_regulatory_effect_license()`.

At current value 3.0 under viable interval [4,8]:
- matched +2 motifs -> YES / `LOWERS_CURRENT_REGULATORY_PRESSURE`;
- crossed -2 motifs -> NO / `WORSENS_CURRENT_REGULATORY_PRESSURE`.

Current value change re-derived the bearing; coordination/value/episode drift withheld it; a multi-value episode abstained with `EXACT_SINGLE_VALUE_BINDING_REQUIRED`.

Earned:
`CURRENT_VALUE_BOUND_EFFECT_EVIDENCE_CAN_DISPOSITION_DISCOVERED_COORDINATION_MOTIFS_USING_EXISTING_REGULATORY_LICENSE_SEMANTICS`.

Scars:
- `REGULATORY_BEARING != SEMANTIC_MEANING`;
- `VALUE_BOUND_EFFECT_LICENSE != EXECUTION_AUTHORITY`;
- `MULTI_VALUE_EPISODE != LICENSE_TO_INVENT_SIGNAL_REFERENT`.

## Branch B — actual signaling / learned response reentry
A concurrent R5 lane, triggered by the same user-level Microseed proceed request, independently advanced the same broad frontier while Branch A was validating.

### Concurrent MS1940 — actual opaque signaling by composition
Durable probe `job-dda9f4cb912b`, then focused clean job `job-18bab4ae0fec`.

A supplied opaque EFFECT token `T0` changed an independently observed counterparty-contingent response under current counterparty/coordination contracts using existing action/outcome machinery.

No production signaling subsystem was required.

Reality-disagreement hostile preserved the failed observation/prediction violation without autonomously rewriting the coordination contract or assigning new token meaning.

Earned:
`OPAQUE_SIGNALING_BY_EXISTING_COMPOSITION` under the bounded synthetic fixture.

### Concurrent MS1941 — learned opaque signal-response reentry
Initial holdout packet job `job-eae464735826` failed exact subject binding because evidence-premise ancestry was omitted: `INVALID_EXPERIMENT_PACKET / SUBJECT_BINDING_INCOMPLETE`.

Corrected packet exposed the next architecture seam: learned relation qualified but could not enter ordinary durable rehearsal because the durable proposal carrier still could not preserve modern evidence-premise ancestry.

Minimum bridge:
- `CounterfactualRehearsalProposal` carries optional evidence-premise epochs/signatures;
- chosen-path rehearsal aggregates exact premise ancestry and fails closed on conflicting epoch/signature rows;
- `QualifiedActionOutcomePredictiveRelation.as_rehearsal_relation()` preserves premise ancestry;
- `counterfactual_rehearsal_status()` rechecks premise qualification, epoch and exact capability signature.

Empty ancestry remains digest-backward-compatible.

Durable bridge probe `job-9f82edc21ae2`: PASS.
Focused clean integration `job-a2d8bbef14e7`: PASS.

Earned:
`LEARNED_OPAQUE_SIGNAL_RESPONSE_REENTERS_REHEARSAL` under the bounded fixture.

Historical scar handling:
`LOSSY_CONVERSION_FORBIDDEN` remains true; old MS1620/MS1779 refusal enforcement is superseded only because the durable carrier is no longer lossy.

Scars:
- `LEARNED_SIGNAL_RESPONSE != TOKEN_MEANING`;
- `LEARNED_ASSOCIATION != REFERENCE`;
- `MODEL_RESOLVED != TRUTH_AUTHORIZED`;
- `REHEARSABLE != EXECUTABLE`.

## Concurrent-source collision / invalidated validator
While Branch A's first MS1942 full validator `job-06ccfb62fc62` was running, Branch B modified the same repo source/test subject.

The validator was terminated immediately after the mutation conflict was detected.

Classification:
`INVALIDATED_WITNESS / SUBJECT_CHANGED_DURING_VALIDATION`.

It is not negative evidence.

Earned process law:
`SOURCE_STABLE_DURING_VALIDATION_IS_A_QUALIFICATION_PREMISE`.

The concurrent MS1941 compatibility validator `job-a6b4b8676926` subsequently completed with:
- 708 nodes collected across broader `tests/` tree;
- 491 passed before timeout groups;
- 0 failed/error;
- compileall PASS;
- source snapshot stable `bb15ebaed039424008fe5a52306af89dfb11fe3cf47598f9578b841b1e3275f9`;
- two large groups timed out;
- classification `NOT_CLOSED`.

One timed group printed 100% completion but did not return before the capture deadline, reinforcing `CHILD_PROCESS_EXIT != PIPE_EOF` / capture-completion ambiguity.

## Merge decision
After all writers stopped, exact worktree diff was audited.

The branches are complementary, not duplicate:
- learned signal-response predictive relation = one subject/evidence/qualification path;
- discovered coordination motif candidate = different subject/evidence/proposal-only path.

Prewritten merge scar:
`DISCOVERED_SIGNAL_MOTIF != QUALIFIED_SIGNAL_RESPONSE_PREDICTIVE_RELATION`.

The merged candidate is co-resident, not fused. No selector converts either representation into token meaning/reference/policy.

## MS1942 minimum embodiment
Production additions from Branch A:
1. `derive_value_bound_candidate_motif_effect(...)` in `microseed/development/discovery.py` — pure read-only exact-source-trace/current-ancestry/value-binding projector.
2. `Microseed.derive_discovered_candidate_regulatory_bearing(...)` — read-only adapter reusing existing regulatory effect-license semantics.
3. status ceilings:
   - `signal_policy_authority: NONE`;
   - `signal_reference_authority: NONE`.

No new registry, message type, signal ontology, reference store, manager, selector, policy persistence or execution route.

Production additions from Branch B remain the MS1941 durable evidence-premise rehearsal-carrier bridge described above.

## Integrated focused validation
Ordinary pytest over MS1940 + MS1941 + MS1942 new milestone files:
job `job-8a310bee20ec` -> **17/17 PASS**.

Historical carrier/value/rehearsal lineage under existing cleanup-neutral teardown shim:
job `job-379ab6da28c8` -> **14/14 PASS** across MS1620, MS1779, MS1780, MS1782, MS1939.

Earlier adjacent Branch-A regression through cleanup-neutral runner:
job `job-83308369276f` -> **37/37 PASS**.

## Integrated full release validation
Durable job:
`job-cad14b4f608e`.

Project-local aggregate receipt:
`reports/ms1942_release_validation/aggregate_receipt.json`
SHA-256:
`6ca2530116da606dbf33aa381dfa50550c7bf2dae077b6c02c9a2828931033d1`.

Final result:
- classification PASS;
- completion COMPLETE;
- exact intended mutation paths: 12;
- collected embodiment nodes: 700;
- covered nodes: 700;
- passed: 700;
- missing: [];
- extra: [];
- duplicate: [];
- negative groups: [];
- terminal unknown groups: [];
- compileall PASS;
- Microseed self-test 81/81 PASS;
- source snapshot start/end exact:
  `bb15ebaed039424008fe5a52306af89dfb11fe3cf47598f9578b841b1e3275f9`;
- Git status subject stable throughout validation;
- recursive timeout splitting closed all test nodes;
- initial groups 119;
- bounded run receipts 127;
- rounds 3.

The cleanup-neutral runner modifies only `TemporaryDirectory.cleanup()` teardown behavior for the known Windows open-SQLite-handle defect; it does not alter Microseed logic, test inputs or assertions.

This is the first admissible integrated release witness for the merged subject.

## Exact merged source hashes before Git seal
- `microseed/development/action_learning.py`: `b23b4bac3d03ce24e23cc82939afae4ece604d3eea325cbb9039418a47f18099`
- `microseed/development/discovery.py`: `701254292d93d34f23aa2242444888698cdd04fb015fba992d5780dad37796c2`
- `microseed/development/rehearsal.py`: `0ad1a60b32d0d6307651ed69a67baf649083485f842233aac7d7af233fccea3d`
- `microseed/runtime/entity.py`: `8da234018e9c4f12c3c95993d858f7590b01ecbc02a5dec2a0247e8e38d95d33`

Methodology/test hashes are retained in the server-side command readback immediately preceding this evidence note.

## Earned developmental state
The merged descendant now supports bounded prelingual signaling substrate in three distinct senses:
1. opaque token emission can participate in a real counterparty-contingent behavior-changing loop;
2. opaque action-response association can be learned from actual admitted outcomes, independently qualified, and re-enter bounded rehearsal;
3. recurrent opaque coordination motifs can be given current regulatory bearing from exact current trace/value ancestry.

It does not establish:
- token meaning;
- reference;
- endogenous token invention;
- endogenous convention creation;
- persistent semantic other-agent identity;
- signal selection authority among multiple YES-bearing candidates;
- execution authority from motif bearing;
- language.

Current scars:
- `SIGNAL != REFERENCE`;
- `TOKEN_EMITTED != TOKEN_MEANS`;
- `LEARNED_SIGNAL_RESPONSE != TOKEN_MEANING`;
- `REGULATORY_BEARING != SEMANTIC_MEANING`;
- `REHEARSABLE != EXECUTABLE`;
- `CANDIDATE_REGULATORY_BEARING != ACTION_INDICATED`;
- `MULTIPLE_YES_BEARING_MOTIFS != SELECTION_AUTHORITY`.

## Next discriminator after Git seal
Predictive/convention currentness:

When repeated external outcomes contradict a previously qualified opaque signal-response relation, can existing predictive-currentness machinery expose that learned operational association as stale while leaving the externally supplied coordination contract untouched and without assigning a new token meaning?

Prewritten boundaries:
- `PREDICTIVE_DRIFT != SEMANTIC_CONVENTION_CHANGE`;
- `STALE_SIGNAL_MODEL != NEW_TOKEN_MEANING`;
- `MODEL_REPLACEMENT != COORDINATION_CONTRACT_REWRITE_AUTHORITY`.

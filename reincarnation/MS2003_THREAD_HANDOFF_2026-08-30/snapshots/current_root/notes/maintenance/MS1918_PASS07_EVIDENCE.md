# MS1918 Pass 7 Evidence — Authenticated Probe Observation Closure

## Discriminator
`FRESHLY_REAUTHORIZED_PROBE_EXECUTION != OBSERVED_OUTCOME_AND_EVIDENCE_CLOSURE`

## Sealed research lineage
- Parent: `a5f6454919f0759e31de85341a856f1542c82018` — MS1917.
- MS1918 experimental seal: `01fc8e0b359d470a0a2ec97ff15685c55b6c7af7`.
- Commit: `MS1918 require authenticated observation for probe evidence`.
- Canonical Main-Dev remains MS1527; no promotion.

## Pre-repair / recovered seam
The restored worktree contained a provisional uncommitted MS1918 production diff plus an untracked hostile suite. Those bytes were treated as recovery evidence, not authority, and inspected before execution.

The ordinary raw outcome route could record an `OBSERVATION_ONLY`-shaped caller object into action closure. That route is retained as explicit assistance/history, but a raw recorded outcome alone was not sufficient evidence that a current qualified observation channel plus bounded-use basis actually authenticated the step.

For a `PROBE_AVAILABLE` epistemic deficit, allowing that raw outcome to drive program-step bearing or completed-program evidence would collapse:
`RECORDED_OUTCOME == AUTHENTICATED_PROGRAM_OBSERVATION`.

## Embodied repair
No new observation subsystem was added. MS1918 consumes the existing MS1749+ authenticated observation admission owner.

Added `_authenticated_probe_program_step_observation(step_record)` in `microseed/runtime/entity.py`.
For `PROBE_AVAILABLE` deficits only:
1. `assess_epistemic_program_step_outcome_bearing(...)` requires the step execution to project through `derive_admitted_opaque_transition_sample(...)` before it may request revisit.
2. `record_completed_epistemic_program_evidence(...)` requires the same authenticated admission before completed program evidence may be recorded/revisit requested.

The existing admission owner independently checks ordinary execution provenance, exact execution/outcome/evidence binding, observation/basis currentness and signatures, scope, action currentness, unique current common operational frame, frame content, and evidence content. MS1918 grants no new truth, answer, qualification, independence, or execution authority.

## Apparatus reduction during validation
The recovered helper also re-compared admitted sample origin/action/end against the step record. A diagnostic source mutant deleting only that comparison SURVIVED because the callers already own exact intent/execution/outcome/content binding before the helper runs.

Disposition: the duplicate comparison was removed rather than defended with a duplicate test.

Scar:
`REDUNDANT_GUARD_SURVIVAL != HIDDEN_HOLE_IF_STRONGER_OWNER_ALREADY_PROVES_THE_SAME_BINDING`.

The final mutation battery therefore contains only load-bearing new branches.

## Hostile surface
Final MS1918 core: 7/7 PASS.

1. Current qualified observation+basis may report an outcome outside the represented branches; actual observation remains sovereign, prediction stays UNKNOWN, completed program evidence records only bounded relevance, and deficit moves to REVISIT_REQUIRED with truth/answer/execution authority NONE.
2. Caller-forged raw outcome may be recorded but cannot become authenticated probe program evidence.
3. Caller-forged raw surprise cannot trigger step-bearing revisit.
4. Authenticated outcome without a unique common current frame remains recorded but cannot become probe program evidence.
5. Observation channel drift after outcome invalidates later program evidence use.
6. Authenticated outcome content remains observation-sovereign even when neither represented branch predicted it.
7. Observation-use basis drift after outcome likewise invalidates later program evidence use.

## Historical owner requalification
MS1912 previously used a manually fabricated action-closure positive scaffold to pressure source-ancestry/content laundering. Under MS1918 that positive scaffold is correctly rejected because it lacks authenticated ordinary observation provenance.

The MS1912 positive/downstream helper was upgraded to the now-earned real path:
current-locus authorized execution → qualified observation/basis → authenticated outcome → COMPLETE trial.

Its original source-ancestry and forged-step hostiles remain intact. Combined MS1912 + MS1918 surface: 12/12 PASS.

## Mutation adequacy
Final frozen source-mutant job: `job-7f1416c383c2`.
Receipt: `reports/ms1918_pass07_source_mutants/receipt.json`.

3/3 load-bearing mutants REJECTED; 0 survivors; 0 unknown:
- `DROP_COMPLETED_PROGRAM_AUTHENTICATED_OBSERVATION_GATE`
- `DROP_STEP_BEARING_AUTHENTICATED_OBSERVATION_GATE`
- `DROP_LIVE_OBSERVATION_AND_BASIS_CURRENTNESS_RECHECK`

The earlier four-mutant diagnostic run is not the final adequacy score because one deliberately diagnostic redundant comparison survived and was subsequently stripped.

## Regression / compatibility
Direct affected-owner job `job-98b0df8bea13`: 48/48 PASS.

Selective regression job `job-988df2b5056d`:
- modern 30/30 PASS;
- inherited cleanup-neutral 74/74 PASS;
- compileall PASS;
- stderr empty.

Frozen exact base verifier job `job-fdb37c17c52a`:
- source stable;
- compileall PASS;
- 176 test files;
- 610 selected PASS;
- zero negative groups;
- six terminal singleton route timeouts only.

Terminal leaf closure job `job-576f33841cb4`:
- exact six terminal files decomposed by test function;
- 53/53 leaf tests PASS;
- no missing/extra/duplicate test nodes;
- zero negative groups;
- zero remaining unknown groups;
- source stable and matched frozen base snapshot `123fdf1f94f6a3c02e8b4370ccec799d3702cf3dbb3a2e9c0fbd9c885ae258a6`.

Final exact compatibility: **663/663 PASS across 176/176 test files**.

## Earned laws
- `RECORDED_OUTCOME != AUTHENTICATED_PROGRAM_OBSERVATION`.
- `AUTHENTICATED_OBSERVATION_AT_ACQUISITION != CURRENTLY_ADMISSIBLE_PROGRAM_EVIDENCE`.
- `PREDICTION != OBSERVATION`.
- `EXECUTION_AUTHORITY != EVIDENCE_AUTHORITY`.
- `COMPLETED_AUTHENTICATED_EXPERIMENT != ANSWER`.
- `RAW_ASSISTANCE_HISTORY != PROBE_REVISIT_AUTHORITY`.

## Claim boundary
MS1918 shows that one freshly reauthorized current-locus direct probe can close through the existing qualified/current observation-use path, carry an actual observation not constrained to predicted branches, and become bounded program evidence/revisit pressure without acquiring truth/answer/execution authority.

It does not establish that the resulting revisit is a lawful model revision, that a successor model is correct, that the deficit is answered, or that any broad novelty claim is earned.

# Current Frontier — MS2004 Unsealed Unified Referent Lifetime Policy

Date: 2026-08-30 ET
Status at checkpoint creation: DIRECT-POSITIVE / UNSEALED / UNPUBLISHED

## Why this is the resume point
MS2003 is the strongest sealed/published research state in this package.
The live worktree contained exactly three untracked MS2004 files, preserved under `unsealed_worktree_snapshot/MS2004/`.

The next thread should therefore resume from MS2004 unless the live server/Git state has advanced beyond this package.

Do not restart at an older milestone merely because older continuity files are stale.

## Exact MS2004 question
From the draft methodology:

> Can the existing Microseed mechanisms compose across a multi-session lifetime in one external world so that drift/relearning, restart/currentness, persisted operational referent classes, zero-row action choice, ambiguity refusal, and capability requalification all coexist without adding a new referent, policy, lifecycle, or cross-cutting manager?

## Existing mechanisms MS2004 composes
The draft intentionally avoids a new cognitive owner.

### Session 1 — historical acquisition
- ordinary lived P-mode outcomes;
- historical qualified action-outcome relations earned from observed history;
- qualification remains external.

### Session 2 — drift/relearning
- N-mode reality creates prediction failures;
- predictive-currentness drift witnesses;
- replacement candidates nominated from lived history;
- later independent outcomes qualify replacements.

### Session 3 — operational referent context
- same external world produces opaque referent probe traces;
- operational referent signatures/classes are persisted via existing evidence ownership;
- operational class-set context is derived;
- existing projection-conditioned relation routing is externally qualified from independent holdouts.

### Session 4 — restart + zero-row policy + requalification coexistence
- another restart;
- unrelated stale capability requalified under MS2000 rules;
- zero supplied rehearsal rows;
- current operational referent class-set context selects already-qualified routing/action program;
- aliased referent surface must fail closed.

## Direct draft result at checkpoint creation
The draft methodology says:
`Current direct result: PASS.`

Observed in the draft:
- 4 sessions / 3 restarts;
- historical acquisition + later drift/relearning use ordinary outcomes;
- P and N operational referent class-set contexts persist as distinct opaque buckets;
- post-restart zero-row action selection chooses `K-17 -> M-23 -> R-41` in both contexts while external consequences remain context-specific (`u` vs `v`);
- aliased current referent context produces no policy;
- capability requalification coexists with authority gain `NONE`;
- identity authority `NONE`.

This is **OBSERVED direct evidence**, not a sealed milestone in this package.

## Candidate earned claim if seal gate passes
The draft proposes:

`FOUR_SESSION_UNIFIED_EXTERNAL_LIFETIME_COMPOSES_DRIFT_RELEARNING_RESTART_CURRENTNESS_PERSISTED_OPERATIONAL_REFERENT_CLASS_PRESSURE_ZERO_ROW_DECISION_POLICY_AMBIGUITY_REFUSAL_AND_CO_PRESENT_CAPABILITY_REQUALIFICATION_WITHOUT_A_NEW_REFERENT_POLICY_OR_LIFECYCLE_MANAGER`.

Do not use this as a verified claim until the current live tree survives the full gate.

## Authority ceiling retained by MS2004 draft
MS2004 does **not** earn:
- semantic reference authority;
- numerical identity authority;
- truth authority;
- self-qualification authority;
- autonomous environment authority;
- autonomous probe-schedule selection;
- general rich-world autonomy.

## Current remaining assistance / substantive blocker
The draft names the remaining assistance explicitly:
- fixed externally equipped opaque referent probe schedule;
- external relation qualification;
- external routing qualification;
- fresh-session environment authority.

Primary next blocker if MS2004 seals:

`FIXED_EXTERNALLY_EQUIPPED_OPAQUE_REFERENT_PROBE_SCHEDULE != ENDOGENOUS_REFERENT_INFORMATION_SEEKING`.

Broader ceiling:

`FOUR_SESSION_UNIFIED_LIFETIME_WITNESS != GENERAL_RICH_WORLD_AUTONOMY`.

## What not to do when resuming MS2004
Do not:
- add a referent manager to make the campaign easier;
- add a lifecycle manager;
- add a semantic policy state;
- feed P/N or referent-class labels into organism-facing code;
- supply the runtime bucket/action/program from the harness;
- call the fixed probe schedule endogenous information seeking;
- call class-set persistence semantic reference;
- call operational persistence numerical identity;
- hide external qualification in a helper and call it autonomy;
- mutate the candidate while a whole-suite witness is running;
- run duplicate whole suites because status transport is noisy.

## Exact seal protocol for MS2004
If live Git still has the three untracked files matching this package:

1. compute and record exact hashes for the three candidate files and any production files they modify/import if changed;
2. direct scratch replay;
3. focused lineage including MS1995, MS1998, MS2000, MS2002, MS2003, MS2004 and relevant restart/currentness/referent tests;
4. self-test;
5. compileall;
6. `git diff --check`;
7. freeze candidate hashes;
8. run **one** whole cleanup-neutral embodiment suite;
9. interpret any failure before patching;
10. post-whole hash readback;
11. stage exact intended paths only;
12. `git diff --cached --check`;
13. local commit/seal;
14. push `research/ms1888-replay`;
15. independent `git ls-remote` readback;
16. only then update continuity/current state and call MS2004 published.

## If the live worktree differs from the packaged MS2004 snapshot
Do not overwrite it with this package.

Instead:
- hash both versions;
- inspect Git status and active jobs;
- determine which version is newer/verified;
- preserve both if lineage is unclear;
- reconcile using evidence/currentness rather than filename or timestamp alone.

## After MS2004
If MS2004 seals without new manager architecture, the next high-value pressure is not “more referent persistence.” It is the missing endogenous information-seeking owner:

Can current epistemic/referent uncertainty plus existing action/program mechanisms **nominate the informative opaque referent probe sequence itself**, under ordinary authorization/currentness/resource law, rather than receiving a fixed probe schedule from the experiment?

That question must preserve:
- `EPISTEMIC_NEED != EXECUTION_PERMISSION`;
- `PROBE_AVAILABLE != PROBE_AUTHORIZED`;
- `PROBE_SEQUENCE_USEFUL != SEMANTIC_REFERENCE`;
- search-budget completeness;
- ambiguity refusal;
- ordinary action authority.

Do not collapse it into the separate NAKED unknown-effect first-probe constitutional issue without explicit analysis. A probe may already be a known/qualified EFFECT action; unknown-effect exploration authority is a different question.
